# 📊 C# 코드 커버리지 측정 따라하기 실습

## Visual Studio 2017 GUI 전용 버전

> **실습 환경**: Visual Studio 2017 (.NET Framework 4.7.2)  
> **도구**: OpenCover + ReportGenerator (NuGet 패키지)  
> **특징**: 명령줄 없이 GUI만으로 모든 작업 수행  
> **예상 소요 시간**: 약 60분

---

## 📋 목차

- Part 1: 코드 커버리지 개념 (Step 1-5)
- Part 2: 프로젝트 설정 (Step 6-15)
- Part 3: 프로덕션 코드 작성 (Step 16-25)
- Part 4: 테스트 코드 작성 (Step 26-35)
- Part 5: 커버리지 측정 실행 (Step 36-45)
- Part 6: 커버리지 개선 (Step 46-55)
- Part 7: 정리 및 베스트 프랙티스 (Step 56-60)

---

# Part 1: 코드 커버리지 개념

---

## 1. 코드 커버리지란?

### Step 1.1: 정의

```
┌────────────────────────────────────────────────────────────────┐
│  코드 커버리지 (Code Coverage)                                 │
├────────────────────────────────────────────────────────────────┤
│                                                                │
│  테스트가 실행될 때 프로덕션 코드의 어느 부분이                │
│  실행되었는지를 측정하는 지표                                  │
│                                                                │
│  ┌──────────────────────────────────────────────────────────┐ │
│  │  public int Add(int a, int b)     ← 테스트가 실행함 ✅   │ │
│  │  {                                                       │ │
│  │      if (a < 0 || b < 0)          ← 실행됨 ✅            │ │
│  │      {                                                   │ │
│  │          throw new Exception();   ← 실행 안 됨 ❌        │ │
│  │      }                                                   │ │
│  │      return a + b;                ← 실행됨 ✅            │ │
│  │  }                                                       │ │
│  └──────────────────────────────────────────────────────────┘ │
│                                                                │
│  위 코드의 커버리지: 4/5 줄 = 80%                              │
│                                                                │
└────────────────────────────────────────────────────────────────┘
```

### Step 1.2: 왜 커버리지를 측정하는가?

```
┌────────────────────────────────────────────────────────────────┐
│  커버리지 측정의 목적                                          │
├────────────────────────────────────────────────────────────────┤
│                                                                │
│  1. 테스트되지 않은 코드 발견                                  │
│     • "이 함수는 테스트가 없네?"                              │
│     • 위험 영역 파악                                          │
│                                                                │
│  2. 테스트 품질 지표                                           │
│     • 팀의 테스트 문화 측정                                   │
│     • 시간에 따른 추이 관찰                                   │
│                                                                │
│  3. 리팩토링 안전망 확인                                       │
│     • "이 코드 고쳐도 될까?"                                  │
│     • 테스트 커버리지가 높으면 자신감 ↑                       │
│                                                                │
│  4. 코드 리뷰 기준                                             │
│     • "새 코드에 테스트가 있나요?"                            │
│     • PR 체크리스트                                           │
│                                                                │
└────────────────────────────────────────────────────────────────┘
```

---

## 2. 커버리지의 종류

### Step 2.1: 커버리지 종류 비교

```
┌────────────────────────────────────────────────────────────────┐
│  커버리지 종류                                                 │
├────────────────────────────────────────────────────────────────┤
│                                                                │
│  1. 라인 커버리지 (Line Coverage)                              │
│     실행된 코드 라인 수 / 전체 코드 라인 수                   │
│     → 가장 기본적, 이해하기 쉬움                              │
│                                                                │
│  2. 브랜치 커버리지 (Branch Coverage)                          │
│     실행된 분기 수 / 전체 분기 수                             │
│     → if/else, switch 등 조건문 분석                          │
│                                                                │
│  3. 메서드 커버리지 (Method Coverage)                          │
│     호출된 메서드 수 / 전체 메서드 수                         │
│     → 대략적인 파악에 유용                                    │
│                                                                │
└────────────────────────────────────────────────────────────────┘
```

### Step 2.2: 커버리지 목표

```
┌────────────────────────────────────────────────────────────────┐
│  권장 커버리지 수준                                            │
├────────────────────────────────────────────────────────────────┤
│                                                                │
│   0%        50%       70%       80%       90%      100%        │
│   ├─────────┼─────────┼─────────┼─────────┼─────────┤         │
│   │         │         │         │         │         │         │
│   │  위험   │  최소   │  권장   │  좋음   │ 이상적  │         │
│   │         │         │         │         │         │         │
│   └─────────┴─────────┴─────────┴─────────┴─────────┘         │
│                                                                │
│  프로젝트 유형별 권장:                                         │
│  • 핵심 비즈니스 로직: 80% 이상                               │
│  • 유틸리티/헬퍼: 70% 이상                                    │
│  • UI 코드: 30-50%                                            │
│  • 레거시 코드: 점진적 증가 목표                              │
│                                                                │
│  ⚠️ 주의: 100% 커버리지 ≠ 좋은 테스트                         │
│  → Assert 없이 실행만 해도 커버리지는 올라감                  │
│  → 테스트의 질(검증)이 더 중요!                               │
│                                                                │
└────────────────────────────────────────────────────────────────┘
```

---

## 3. Visual Studio 2017에서 사용할 도구

### Step 3.1: 도구 선택

```
┌────────────────────────────────────────────────────────────────┐
│  VS 2017용 코드 커버리지 도구                                  │
├────────────────────────────────────────────────────────────────┤
│                                                                │
│  ❌ Coverlet                                                   │
│     • .NET Core / .NET 5+ 전용                                │
│     • .NET Framework 4.x에서 제한적                           │
│                                                                │
│  ❌ VS Enterprise 내장 커버리지                                │
│     • Enterprise 버전에서만 사용 가능                         │
│     • Community/Professional 버전 불가                        │
│                                                                │
│  ✅ OpenCover + ReportGenerator (선택!)                       │
│     • 무료, 오픈소스                                          │
│     • .NET Framework 완벽 지원                                │
│     • NuGet으로 간단 설치                                     │
│     • HTML 리포트 생성 가능                                   │
│                                                                │
│  ✅ AxoCover (VS 확장 프로그램)                                │
│     • Visual Studio에서 직접 커버리지 확인                    │
│     • OpenCover 기반                                          │
│     • 코드에 색상 표시                                        │
│                                                                │
└────────────────────────────────────────────────────────────────┘
```

---

# Part 2: 프로젝트 설정

---

## 4. 솔루션 생성

### Step 4.1: Visual Studio 2017 실행

```
1. Visual Studio 2017 실행
2. 시작 화면에서 "새 프로젝트 만들기" 클릭
```

### Step 4.2: 솔루션 생성

```
┌────────────────────────────────────────────────────────────────┐
│  새 프로젝트 만들기                                            │
├────────────────────────────────────────────────────────────────┤
│                                                                │
│  1. 파일 → 새로 만들기 → 프로젝트 (Ctrl + Shift + N)          │
│                                                                │
│  2. 왼쪽 패널:                                                 │
│     Visual C# → Windows 데스크톱                              │
│                                                                │
│  3. 가운데 패널:                                               │
│     "클래스 라이브러리(.NET Framework)" 선택                  │
│                                                                │
│  4. 아래쪽 설정:                                               │
│     • 이름: CoverageDemo                                      │
│     • 위치: C:\Users\[사용자]\source\repos                    │
│     • 솔루션 이름: CoverageDemo                               │
│     • 프레임워크: .NET Framework 4.7.2                        │
│     • ☑ 솔루션용 디렉터리 만들기                              │
│                                                                │
│  5. "확인" 클릭                                                │
│                                                                │
└────────────────────────────────────────────────────────────────┘
```

### Step 4.3: 기본 파일 삭제

```
┌────────────────────────────────────────────────────────────────┐
│  Class1.cs 삭제                                                │
├────────────────────────────────────────────────────────────────┤
│                                                                │
│  솔루션 탐색기에서:                                            │
│                                                                │
│  CoverageDemo (솔루션)                                         │
│  └── CoverageDemo (프로젝트)                                   │
│      └── Class1.cs  ← 우클릭 → 삭제                           │
│                                                                │
│  "확인" 클릭하여 삭제                                          │
│                                                                │
└────────────────────────────────────────────────────────────────┘
```

---

## 5. 테스트 프로젝트 추가

### Step 5.1: 테스트 프로젝트 생성

```
┌────────────────────────────────────────────────────────────────┐
│  단위 테스트 프로젝트 추가                                     │
├────────────────────────────────────────────────────────────────┤
│                                                                │
│  1. 솔루션 탐색기에서 솔루션 "CoverageDemo" 우클릭             │
│                                                                │
│  2. 추가 → 새 프로젝트                                        │
│                                                                │
│  3. 왼쪽 패널:                                                 │
│     Visual C# → 테스트                                        │
│                                                                │
│  4. 가운데 패널:                                               │
│     "단위 테스트 프로젝트(.NET Framework)" 선택               │
│                                                                │
│  5. 아래쪽 설정:                                               │
│     • 이름: CoverageDemo.Tests                                │
│     • 프레임워크: .NET Framework 4.7.2                        │
│                                                                │
│  6. "확인" 클릭                                                │
│                                                                │
└────────────────────────────────────────────────────────────────┘
```

### Step 5.2: 프로젝트 참조 추가

```
┌────────────────────────────────────────────────────────────────┐
│  프로덕션 프로젝트 참조 추가                                   │
├────────────────────────────────────────────────────────────────┤
│                                                                │
│  1. 솔루션 탐색기에서:                                         │
│     CoverageDemo.Tests → 참조 → 우클릭 → 참조 추가            │
│                                                                │
│  2. 참조 관리자 창:                                            │
│     왼쪽에서 "프로젝트" 선택                                  │
│                                                                │
│  3. "CoverageDemo" 체크 ☑                                     │
│                                                                │
│  4. "확인" 클릭                                                │
│                                                                │
│  결과:                                                         │
│  CoverageDemo.Tests                                            │
│  └── 참조                                                      │
│      └── CoverageDemo  ← 추가됨!                              │
│                                                                │
└────────────────────────────────────────────────────────────────┘
```

---

## 6. NuGet 패키지 설치

### Step 6.1: NuGet 패키지 관리자 열기

```
┌────────────────────────────────────────────────────────────────┐
│  NuGet 패키지 관리자 열기                                      │
├────────────────────────────────────────────────────────────────┤
│                                                                │
│  방법 1: 메뉴                                                  │
│  도구 → NuGet 패키지 관리자 → 솔루션용 NuGet 패키지 관리      │
│                                                                │
│  방법 2: 솔루션 탐색기                                         │
│  솔루션 우클릭 → NuGet 패키지 관리                            │
│                                                                │
└────────────────────────────────────────────────────────────────┘
```

### Step 6.2: 테스트 프로젝트에 NUnit 설치

```
┌────────────────────────────────────────────────────────────────┐
│  NUnit 패키지 설치 (CoverageDemo.Tests 프로젝트)               │
├────────────────────────────────────────────────────────────────┤
│                                                                │
│  1. "찾아보기" 탭 클릭                                         │
│                                                                │
│  2. 검색창에 "NUnit" 입력                                      │
│                                                                │
│  3. 설치할 패키지:                                             │
│     ┌────────────────────────────────────────────────────┐    │
│     │ 패키지명              │ 버전     │ 대상 프로젝트   │    │
│     ├───────────────────────┼──────────┼─────────────────┤    │
│     │ NUnit                 │ 3.12.0   │ CoverageDemo.   │    │
│     │                       │          │ Tests           │    │
│     ├───────────────────────┼──────────┼─────────────────┤    │
│     │ NUnit3TestAdapter     │ 3.17.0   │ CoverageDemo.   │    │
│     │                       │          │ Tests           │    │
│     └────────────────────────────────────────────────────┘    │
│                                                                │
│  4. 오른쪽에서 "CoverageDemo.Tests" 체크 ☑                    │
│                                                                │
│  5. "설치" 클릭                                                │
│                                                                │
│  6. 라이선스 동의 → "동의함" 클릭                             │
│                                                                │
└────────────────────────────────────────────────────────────────┘
```

### Step 6.3: OpenCover 설치

```
┌────────────────────────────────────────────────────────────────┐
│  OpenCover 패키지 설치 (CoverageDemo.Tests 프로젝트)           │
├────────────────────────────────────────────────────────────────┤
│                                                                │
│  1. 검색창에 "OpenCover" 입력                                  │
│                                                                │
│  2. "OpenCover" 선택 (by OpenCover)                           │
│     버전: 4.7.1221 (또는 최신)                                │
│                                                                │
│  3. 오른쪽에서 "CoverageDemo.Tests" 체크 ☑                    │
│                                                                │
│  4. "설치" 클릭                                                │
│                                                                │
│  ⚠️ 참고: OpenCover는 .NET Framework 전용 도구입니다          │
│                                                                │
└────────────────────────────────────────────────────────────────┘
```

### Step 6.4: ReportGenerator 설치

```
┌────────────────────────────────────────────────────────────────┐
│  ReportGenerator 패키지 설치 (CoverageDemo.Tests 프로젝트)     │
├────────────────────────────────────────────────────────────────┤
│                                                                │
│  1. 검색창에 "ReportGenerator" 입력                            │
│                                                                │
│  2. "ReportGenerator" 선택 (by Daniel Palme)                  │
│     버전: 4.8.13 (VS 2017 호환 버전)                          │
│                                                                │
│  3. 오른쪽에서 "CoverageDemo.Tests" 체크 ☑                    │
│                                                                │
│  4. "설치" 클릭                                                │
│                                                                │
└────────────────────────────────────────────────────────────────┘
```

### Step 6.5: 설치 확인

```
┌────────────────────────────────────────────────────────────────┐
│  설치된 패키지 확인                                            │
├────────────────────────────────────────────────────────────────┤
│                                                                │
│  NuGet 패키지 관리자 → "설치됨" 탭 클릭                        │
│                                                                │
│  CoverageDemo.Tests에 설치된 패키지:                           │
│  ┌────────────────────────────────────────────────────────┐   │
│  │ ✅ NUnit                    3.12.0                     │   │
│  │ ✅ NUnit3TestAdapter        3.17.0                     │   │
│  │ ✅ OpenCover                4.7.1221                   │   │
│  │ ✅ ReportGenerator          4.8.13                     │   │
│  │ ✅ Microsoft.NET.Test.Sdk   16.9.4                     │   │
│  └────────────────────────────────────────────────────────┘   │
│                                                                │
└────────────────────────────────────────────────────────────────┘
```

---

## 7. 현재 솔루션 구조

### Step 7.1: 솔루션 탐색기 확인

```
┌────────────────────────────────────────────────────────────────┐
│  솔루션 구조                                                   │
├────────────────────────────────────────────────────────────────┤
│                                                                │
│  솔루션 'CoverageDemo' (2개 프로젝트)                          │
│  │                                                             │
│  ├── CoverageDemo                    ← 프로덕션 프로젝트       │
│  │   ├── Properties                                            │
│  │   │   └── AssemblyInfo.cs                                   │
│  │   ├── 참조                                                  │
│  │   └── (코드 파일을 추가할 예정)                             │
│  │                                                             │
│  └── CoverageDemo.Tests              ← 테스트 프로젝트         │
│      ├── Properties                                            │
│      │   └── AssemblyInfo.cs                                   │
│      ├── 참조                                                  │
│      │   └── CoverageDemo                                      │
│      ├── packages.config             ← NuGet 패키지 목록       │
│      └── UnitTest1.cs                ← 기본 테스트 파일        │
│                                                                │
└────────────────────────────────────────────────────────────────┘
```

---

# Part 3: 프로덕션 코드 작성

---

## 8. Calculator 클래스 작성

### Step 8.1: 새 클래스 추가

```
┌────────────────────────────────────────────────────────────────┐
│  Calculator.cs 추가                                            │
├────────────────────────────────────────────────────────────────┤
│                                                                │
│  1. 솔루션 탐색기에서:                                         │
│     CoverageDemo 프로젝트 우클릭                               │
│                                                                │
│  2. 추가 → 클래스                                             │
│                                                                │
│  3. 이름: Calculator.cs                                        │
│                                                                │
│  4. "추가" 클릭                                                │
│                                                                │
└────────────────────────────────────────────────────────────────┘
```

### Step 8.2: Calculator.cs 코드 작성

```csharp
// CoverageDemo/Calculator.cs
using System;

namespace CoverageDemo
{
    /// <summary>
    /// 기본 계산기 클래스
    /// </summary>
    public class Calculator
    {
        /// <summary>
        /// 두 수를 더합니다.
        /// </summary>
        public int Add(int a, int b)
        {
            return a + b;
        }

        /// <summary>
        /// 두 수를 뺍니다.
        /// </summary>
        public int Subtract(int a, int b)
        {
            return a - b;
        }

        /// <summary>
        /// 두 수를 곱합니다.
        /// </summary>
        public int Multiply(int a, int b)
        {
            return a * b;
        }

        /// <summary>
        /// 두 수를 나눕니다.
        /// </summary>
        public double Divide(int a, int b)
        {
            if (b == 0)
            {
                throw new DivideByZeroException("0으로 나눌 수 없습니다.");
            }

            return (double)a / b;
        }

        /// <summary>
        /// 팩토리얼을 계산합니다.
        /// </summary>
        public int Factorial(int n)
        {
            if (n < 0)
            {
                throw new ArgumentException("음수의 팩토리얼은 계산할 수 없습니다.");
            }

            if (n == 0 || n == 1)
            {
                return 1;
            }

            int result = 1;
            for (int i = 2; i <= n; i++)
            {
                result *= i;
            }

            return result;
        }

        /// <summary>
        /// 짝수인지 확인합니다.
        /// </summary>
        public bool IsEven(int number)
        {
            return number % 2 == 0;
        }

        /// <summary>
        /// 소수인지 확인합니다.
        /// </summary>
        public bool IsPrime(int number)
        {
            if (number < 2)
            {
                return false;
            }

            if (number == 2)
            {
                return true;
            }

            if (number % 2 == 0)
            {
                return false;
            }

            for (int i = 3; i <= Math.Sqrt(number); i += 2)
            {
                if (number % i == 0)
                {
                    return false;
                }
            }

            return true;
        }
    }
}
```

---

## 9. StringUtils 클래스 작성

### Step 9.1: 새 클래스 추가

```
┌────────────────────────────────────────────────────────────────┐
│  StringUtils.cs 추가                                           │
├────────────────────────────────────────────────────────────────┤
│                                                                │
│  1. CoverageDemo 프로젝트 우클릭                               │
│                                                                │
│  2. 추가 → 클래스                                             │
│                                                                │
│  3. 이름: StringUtils.cs                                       │
│                                                                │
│  4. "추가" 클릭                                                │
│                                                                │
└────────────────────────────────────────────────────────────────┘
```

### Step 9.2: StringUtils.cs 코드 작성

```csharp
// CoverageDemo/StringUtils.cs
using System;

namespace CoverageDemo
{
    /// <summary>
    /// 문자열 유틸리티 클래스
    /// </summary>
    public class StringUtils
    {
        /// <summary>
        /// 문자열을 뒤집습니다.
        /// </summary>
        public string Reverse(string input)
        {
            if (string.IsNullOrEmpty(input))
            {
                return input;
            }

            char[] charArray = input.ToCharArray();
            Array.Reverse(charArray);
            return new string(charArray);
        }

        /// <summary>
        /// 회문인지 확인합니다.
        /// </summary>
        public bool IsPalindrome(string input)
        {
            if (string.IsNullOrEmpty(input))
            {
                return true;
            }

            string cleaned = input.ToLower().Replace(" ", "");
            string reversed = Reverse(cleaned);

            return cleaned == reversed;
        }

        /// <summary>
        /// 단어 수를 셉니다.
        /// </summary>
        public int CountWords(string input)
        {
            if (string.IsNullOrWhiteSpace(input))
            {
                return 0;
            }

            return input.Split(new[] { ' ', '\t', '\n' },
                              StringSplitOptions.RemoveEmptyEntries).Length;
        }

        /// <summary>
        /// 지정 길이로 자르고 말줄임표를 붙입니다.
        /// </summary>
        public string TruncateWithEllipsis(string input, int maxLength)
        {
            if (string.IsNullOrEmpty(input))
            {
                return input;
            }

            if (maxLength < 3)
            {
                throw new ArgumentException("최소 길이는 3 이상이어야 합니다.");
            }

            if (input.Length <= maxLength)
            {
                return input;
            }

            return input.Substring(0, maxLength - 3) + "...";
        }

        /// <summary>
        /// Title Case로 변환합니다.
        /// </summary>
        public string ToTitleCase(string input)
        {
            if (string.IsNullOrEmpty(input))
            {
                return input;
            }

            var words = input.ToLower().Split(' ');

            for (int i = 0; i < words.Length; i++)
            {
                if (words[i].Length > 0)
                {
                    words[i] = char.ToUpper(words[i][0]) + words[i].Substring(1);
                }
            }

            return string.Join(" ", words);
        }
    }
}
```

---

## 10. UserService 클래스 작성

### Step 10.1: 새 클래스 추가

```
┌────────────────────────────────────────────────────────────────┐
│  UserService.cs 추가                                           │
├────────────────────────────────────────────────────────────────┤
│                                                                │
│  1. CoverageDemo 프로젝트 우클릭                               │
│                                                                │
│  2. 추가 → 클래스                                             │
│                                                                │
│  3. 이름: UserService.cs                                       │
│                                                                │
│  4. "추가" 클릭                                                │
│                                                                │
└────────────────────────────────────────────────────────────────┘
```

### Step 10.2: UserService.cs 코드 작성

```csharp
// CoverageDemo/UserService.cs
using System;
using System.Collections.Generic;
using System.Linq;

namespace CoverageDemo
{
    /// <summary>
    /// 사용자 모델
    /// </summary>
    public class User
    {
        public int Id { get; set; }
        public string Name { get; set; }
        public string Email { get; set; }
        public int Age { get; set; }
        public bool IsActive { get; set; }

        public User()
        {
            Name = string.Empty;
            Email = string.Empty;
            IsActive = true;
        }
    }

    /// <summary>
    /// 사용자 서비스 클래스
    /// </summary>
    public class UserService
    {
        private readonly List<User> _users = new List<User>();

        /// <summary>
        /// ID로 사용자를 조회합니다.
        /// </summary>
        public User GetById(int id)
        {
            return _users.FirstOrDefault(u => u.Id == id);
        }

        /// <summary>
        /// 모든 사용자를 조회합니다.
        /// </summary>
        public IEnumerable<User> GetAll()
        {
            return _users.ToList();
        }

        /// <summary>
        /// 활성 사용자만 조회합니다.
        /// </summary>
        public IEnumerable<User> GetActiveUsers()
        {
            return _users.Where(u => u.IsActive).ToList();
        }

        /// <summary>
        /// 성인 사용자만 조회합니다.
        /// </summary>
        public IEnumerable<User> GetAdultUsers()
        {
            return _users.Where(u => u.Age >= 18).ToList();
        }

        /// <summary>
        /// 사용자를 추가합니다.
        /// </summary>
        public void Add(User user)
        {
            if (user == null)
            {
                throw new ArgumentNullException("user");
            }

            if (string.IsNullOrWhiteSpace(user.Name))
            {
                throw new ArgumentException("이름은 필수입니다.");
            }

            if (string.IsNullOrWhiteSpace(user.Email))
            {
                throw new ArgumentException("이메일은 필수입니다.");
            }

            if (!IsValidEmail(user.Email))
            {
                throw new ArgumentException("올바른 이메일 형식이 아닙니다.");
            }

            if (_users.Any(u => u.Email == user.Email))
            {
                throw new InvalidOperationException("이미 존재하는 이메일입니다.");
            }

            user.Id = _users.Count > 0 ? _users.Max(u => u.Id) + 1 : 1;
            _users.Add(user);
        }

        /// <summary>
        /// 사용자를 삭제합니다.
        /// </summary>
        public bool Delete(int id)
        {
            var user = GetById(id);
            if (user == null)
            {
                return false;
            }

            return _users.Remove(user);
        }

        /// <summary>
        /// 사용자를 비활성화합니다.
        /// </summary>
        public void Deactivate(int id)
        {
            var user = GetById(id);
            if (user == null)
            {
                throw new InvalidOperationException("사용자를 찾을 수 없습니다.");
            }

            user.IsActive = false;
        }

        /// <summary>
        /// 이메일 형식을 검증합니다.
        /// </summary>
        private bool IsValidEmail(string email)
        {
            return email.Contains("@") && email.Contains(".");
        }
    }
}
```

---

## 11. 빌드 확인

### Step 11.1: 솔루션 빌드

```
┌────────────────────────────────────────────────────────────────┐
│  솔루션 빌드                                                   │
├────────────────────────────────────────────────────────────────┤
│                                                                │
│  1. 메뉴: 빌드 → 솔루션 빌드                                  │
│     또는 단축키: Ctrl + Shift + B                             │
│                                                                │
│  2. 출력 창 확인:                                              │
│     ========== 빌드: 성공 2, 실패 0, 최신 0, 생략 0 ==========│
│                                                                │
│  ✅ 빌드 성공!                                                 │
│                                                                │
└────────────────────────────────────────────────────────────────┘
```

### Step 11.2: 현재 프로덕션 코드 요약

```
┌────────────────────────────────────────────────────────────────┐
│  프로덕션 코드 현황                                            │
├────────────────────────────────────────────────────────────────┤
│                                                                │
│  CoverageDemo 프로젝트                                         │
│  │                                                             │
│  ├── Calculator.cs (7개 메서드)                                │
│  │   ├── Add()                                                 │
│  │   ├── Subtract()                                            │
│  │   ├── Multiply()                                            │
│  │   ├── Divide()                                              │
│  │   ├── Factorial()                                           │
│  │   ├── IsEven()                                              │
│  │   └── IsPrime()                                             │
│  │                                                             │
│  ├── StringUtils.cs (5개 메서드)                               │
│  │   ├── Reverse()                                             │
│  │   ├── IsPalindrome()                                        │
│  │   ├── CountWords()                                          │
│  │   ├── TruncateWithEllipsis()                                │
│  │   └── ToTitleCase()                                         │
│  │                                                             │
│  └── UserService.cs (8개 메서드)                               │
│      ├── GetById()                                             │
│      ├── GetAll()                                              │
│      ├── GetActiveUsers()                                      │
│      ├── GetAdultUsers()                                       │
│      ├── Add()                                                 │
│      ├── Delete()                                              │
│      ├── Deactivate()                                          │
│      └── IsValidEmail() (private)                              │
│                                                                │
│  총 20개 메서드 (테스트 대상)                                  │
│                                                                │
└────────────────────────────────────────────────────────────────┘
```

---

# Part 4: 테스트 코드 작성 (일부만)

---

## 12. 기본 테스트 파일 수정

### Step 12.1: UnitTest1.cs 삭제

```
┌────────────────────────────────────────────────────────────────┐
│  기본 테스트 파일 삭제                                         │
├────────────────────────────────────────────────────────────────┤
│                                                                │
│  솔루션 탐색기에서:                                            │
│  CoverageDemo.Tests → UnitTest1.cs → 우클릭 → 삭제            │
│                                                                │
└────────────────────────────────────────────────────────────────┘
```

---

## 13. CalculatorTests 작성 (일부만)

### Step 13.1: 테스트 클래스 추가

```
┌────────────────────────────────────────────────────────────────┐
│  CalculatorTests.cs 추가                                       │
├────────────────────────────────────────────────────────────────┤
│                                                                │
│  1. CoverageDemo.Tests 프로젝트 우클릭                         │
│                                                                │
│  2. 추가 → 클래스                                             │
│                                                                │
│  3. 이름: CalculatorTests.cs                                   │
│                                                                │
│  4. "추가" 클릭                                                │
│                                                                │
└────────────────────────────────────────────────────────────────┘
```

### Step 13.2: CalculatorTests.cs 코드 (일부 테스트만!)

```csharp
// CoverageDemo.Tests/CalculatorTests.cs
using NUnit.Framework;

namespace CoverageDemo.Tests
{
    /// <summary>
    /// Calculator 테스트 클래스
    /// ⚠️ 의도적으로 일부 테스트만 작성 (커버리지 측정을 위해)
    /// </summary>
    [TestFixture]
    public class CalculatorTests
    {
        private Calculator _calculator;

        [SetUp]
        public void SetUp()
        {
            _calculator = new Calculator();
        }

        #region Add 테스트 - 작성됨 ✅

        [Test]
        public void Add_TwoPositiveNumbers_ReturnsSum()
        {
            // Act
            var result = _calculator.Add(2, 3);

            // Assert
            Assert.AreEqual(5, result);
        }

        [Test]
        public void Add_NegativeNumbers_ReturnsSum()
        {
            // Act
            var result = _calculator.Add(-2, -3);

            // Assert
            Assert.AreEqual(-5, result);
        }

        #endregion

        #region Subtract 테스트 - 작성됨 ✅

        [Test]
        public void Subtract_TwoNumbers_ReturnsDifference()
        {
            // Act
            var result = _calculator.Subtract(5, 3);

            // Assert
            Assert.AreEqual(2, result);
        }

        #endregion

        #region Multiply 테스트 - 미작성! ❌
        // 커버리지 측정 시 이 부분이 빨간색으로 표시될 것
        #endregion

        #region Divide 테스트 - 일부만 작성 ⚠️

        [Test]
        public void Divide_TwoNumbers_ReturnsQuotient()
        {
            // Act
            var result = _calculator.Divide(10, 2);

            // Assert
            Assert.AreEqual(5.0, result);
        }

        // ❌ 0으로 나누기 테스트 - 미작성!
        // 예외 처리 부분이 커버되지 않음

        #endregion

        #region Factorial 테스트 - 미작성! ❌
        // 전체 메서드가 커버되지 않음
        #endregion

        #region IsEven 테스트 - 미작성! ❌
        #endregion

        #region IsPrime 테스트 - 미작성! ❌
        #endregion
    }
}
```

---

## 14. StringUtilsTests 작성 (일부만)

### Step 14.1: 테스트 클래스 추가

```
┌────────────────────────────────────────────────────────────────┐
│  StringUtilsTests.cs 추가                                      │
├────────────────────────────────────────────────────────────────┤
│                                                                │
│  1. CoverageDemo.Tests 프로젝트 우클릭                         │
│                                                                │
│  2. 추가 → 클래스                                             │
│                                                                │
│  3. 이름: StringUtilsTests.cs                                  │
│                                                                │
│  4. "추가" 클릭                                                │
│                                                                │
└────────────────────────────────────────────────────────────────┘
```

### Step 14.2: StringUtilsTests.cs 코드 (일부 테스트만!)

```csharp
// CoverageDemo.Tests/StringUtilsTests.cs
using NUnit.Framework;

namespace CoverageDemo.Tests
{
    /// <summary>
    /// StringUtils 테스트 클래스
    /// ⚠️ 의도적으로 일부 테스트만 작성 (커버리지 측정을 위해)
    /// </summary>
    [TestFixture]
    public class StringUtilsTests
    {
        private StringUtils _stringUtils;

        [SetUp]
        public void SetUp()
        {
            _stringUtils = new StringUtils();
        }

        #region Reverse 테스트 - 일부만 작성 ⚠️

        [Test]
        public void Reverse_NormalString_ReturnsReversed()
        {
            // Act
            var result = _stringUtils.Reverse("hello");

            // Assert
            Assert.AreEqual("olleh", result);
        }

        // ❌ null, empty 테스트 - 미작성!

        #endregion

        #region IsPalindrome 테스트 - 미작성! ❌
        #endregion

        #region CountWords 테스트 - 미작성! ❌
        #endregion

        #region TruncateWithEllipsis 테스트 - 미작성! ❌
        #endregion

        #region ToTitleCase 테스트 - 미작성! ❌
        #endregion
    }
}
```

---

## 15. 테스트 실행

### Step 15.1: 테스트 탐색기 열기

```
┌────────────────────────────────────────────────────────────────┐
│  테스트 탐색기 열기                                            │
├────────────────────────────────────────────────────────────────┤
│                                                                │
│  메뉴: 테스트 → 창 → 테스트 탐색기                            │
│  또는 단축키: Ctrl + E, T                                     │
│                                                                │
│  ┌────────────────────────────────────────────────────────┐   │
│  │  테스트 탐색기                                 - □ X   │   │
│  ├────────────────────────────────────────────────────────┤   │
│  │  🔍 검색                                               │   │
│  ├────────────────────────────────────────────────────────┤   │
│  │  ▶ 모두 실행 | 🔄 | ⚙                                 │   │
│  ├────────────────────────────────────────────────────────┤   │
│  │  ├── CalculatorTests (4)                               │   │
│  │  │   ├── Add_TwoPositiveNumbers_ReturnsSum            │   │
│  │  │   ├── Add_NegativeNumbers_ReturnsSum               │   │
│  │  │   ├── Subtract_TwoNumbers_ReturnsDifference        │   │
│  │  │   └── Divide_TwoNumbers_ReturnsQuotient            │   │
│  │  │                                                     │   │
│  │  └── StringUtilsTests (1)                              │   │
│  │      └── Reverse_NormalString_ReturnsReversed         │   │
│  └────────────────────────────────────────────────────────┘   │
│                                                                │
└────────────────────────────────────────────────────────────────┘
```

### Step 15.2: 테스트 실행

```
┌────────────────────────────────────────────────────────────────┐
│  테스트 실행                                                   │
├────────────────────────────────────────────────────────────────┤
│                                                                │
│  1. 테스트 탐색기에서 "모두 실행" 클릭                         │
│     또는 단축키: Ctrl + R, A                                  │
│                                                                │
│  2. 결과 확인:                                                 │
│     ┌────────────────────────────────────────────────────┐    │
│     │  ✅ 통과: 5                                         │    │
│     │  ❌ 실패: 0                                         │    │
│     │  ⏭ 건너뜀: 0                                        │    │
│     │  ⏱ 시간: 0.5초                                      │    │
│     └────────────────────────────────────────────────────┘    │
│                                                                │
│  모든 테스트 통과! ✅                                          │
│                                                                │
└────────────────────────────────────────────────────────────────┘
```

### Step 15.3: 현재 테스트 현황

```
┌────────────────────────────────────────────────────────────────┐
│  테스트 현황 요약                                              │
├────────────────────────────────────────────────────────────────┤
│                                                                │
│  작성된 테스트:                                                │
│  ┌──────────────────┬──────────────┬──────────────────────┐   │
│  │ 클래스           │ 메서드 수    │ 테스트 수            │   │
│  ├──────────────────┼──────────────┼──────────────────────┤   │
│  │ Calculator       │ 7개          │ 4개 (일부만)        │   │
│  │ StringUtils      │ 5개          │ 1개 (일부만)        │   │
│  │ UserService      │ 8개          │ 0개 (미작성)        │   │
│  ├──────────────────┼──────────────┼──────────────────────┤   │
│  │ 합계             │ 20개         │ 5개                 │   │
│  └──────────────────┴──────────────┴──────────────────────┘   │
│                                                                │
│  → 커버리지가 매우 낮을 것으로 예상 (약 20-30%?)              │
│                                                                │
└────────────────────────────────────────────────────────────────┘
```

---

# Part 5: 커버리지 측정 실행

---

## 16. AxoCover 확장 프로그램 설치 (권장)

### Step 16.1: 확장 프로그램 설치

```
┌────────────────────────────────────────────────────────────────┐
│  AxoCover 설치 (Visual Studio 확장)                            │
├────────────────────────────────────────────────────────────────┤
│                                                                │
│  1. 메뉴: 도구 → 확장 및 업데이트                             │
│                                                                │
│  2. 왼쪽에서 "온라인" 선택                                    │
│                                                                │
│  3. 검색창에 "AxoCover" 입력                                  │
│                                                                │
│  4. "AxoCover" 찾아서 "다운로드" 클릭                         │
│                                                                │
│  5. Visual Studio 종료 (설치 진행됨)                          │
│                                                                │
│  6. VSIX 설치 마법사에서 "수정" 클릭                          │
│                                                                │
│  7. Visual Studio 다시 실행                                   │
│                                                                │
│  ⚠️ 참고: VS 2017 버전에 맞는 AxoCover가 필요합니다          │
│                                                                │
└────────────────────────────────────────────────────────────────┘
```

### Step 16.2: AxoCover 창 열기

```
┌────────────────────────────────────────────────────────────────┐
│  AxoCover 창 열기                                              │
├────────────────────────────────────────────────────────────────┤
│                                                                │
│  메뉴: 보기 → 다른 창 → AxoCover                              │
│                                                                │
│  또는 메뉴: 도구 → AxoCover                                   │
│                                                                │
│  ┌────────────────────────────────────────────────────────┐   │
│  │  AxoCover                                      - □ X   │   │
│  ├────────────────────────────────────────────────────────┤   │
│  │                                                        │   │
│  │  [▶ Run Tests with Coverage]                          │   │
│  │                                                        │   │
│  │  Coverage Results:                                     │   │
│  │  (테스트 실행 후 표시됨)                               │   │
│  │                                                        │   │
│  └────────────────────────────────────────────────────────┘   │
│                                                                │
└────────────────────────────────────────────────────────────────┘
```

---

## 17. 대안: 수동으로 OpenCover 실행

### Step 17.1: 배치 파일 생성 (수동 방법)

AxoCover가 설치되지 않는 경우, 배치 파일로 커버리지를 측정할 수 있습니다.

```
┌────────────────────────────────────────────────────────────────┐
│  RunCoverage.bat 파일 생성                                     │
├────────────────────────────────────────────────────────────────┤
│                                                                │
│  1. 솔루션 폴더에 새 텍스트 파일 생성                          │
│     (솔루션 탐색기 → 솔루션 우클릭 → 파일 탐색기에서 폴더 열기)│
│                                                                │
│  2. 파일 이름: RunCoverage.bat                                 │
│                                                                │
└────────────────────────────────────────────────────────────────┘
```

### Step 17.2: RunCoverage.bat 내용

```batch
@echo off
echo ========================================
echo  코드 커버리지 측정 시작
echo ========================================

REM 경로 설정
set OPENCOVER=packages\OpenCover.4.7.1221\tools\OpenCover.Console.exe
set REPORTGEN=packages\ReportGenerator.4.8.13\tools\net47\ReportGenerator.exe
set NUNIT=packages\NUnit.ConsoleRunner.3.12.0\tools\nunit3-console.exe
set TEST_DLL=CoverageDemo.Tests\bin\Debug\CoverageDemo.Tests.dll
set COVERAGE_DIR=CoverageReport

REM 이전 결과 삭제
if exist coverage.xml del coverage.xml
if exist %COVERAGE_DIR% rmdir /s /q %COVERAGE_DIR%

REM OpenCover로 커버리지 수집
echo.
echo [1/2] 커버리지 수집 중...
%OPENCOVER% ^
  -target:"%NUNIT%" ^
  -targetargs:"%TEST_DLL%" ^
  -output:coverage.xml ^
  -filter:"+[CoverageDemo]* -[CoverageDemo.Tests]*" ^
  -register:user

REM ReportGenerator로 HTML 리포트 생성
echo.
echo [2/2] HTML 리포트 생성 중...
%REPORTGEN% ^
  -reports:coverage.xml ^
  -targetdir:%COVERAGE_DIR% ^
  -reporttypes:Html

REM 리포트 열기
echo.
echo ========================================
echo  커버리지 리포트 생성 완료!
echo ========================================
start %COVERAGE_DIR%\index.html

pause
```

### Step 17.3: NUnit Console Runner 설치 필요

```
┌────────────────────────────────────────────────────────────────┐
│  NUnit.ConsoleRunner 패키지 설치                               │
├────────────────────────────────────────────────────────────────┤
│                                                                │
│  배치 파일 방식을 사용하려면 NUnit Console Runner가 필요합니다 │
│                                                                │
│  1. NuGet 패키지 관리자 열기                                   │
│                                                                │
│  2. 검색: "NUnit.ConsoleRunner"                               │
│                                                                │
│  3. 설치 (솔루션 레벨 또는 테스트 프로젝트)                   │
│                                                                │
└────────────────────────────────────────────────────────────────┘
```

---

## 18. AxoCover로 커버리지 측정

### Step 18.1: 커버리지 실행

```
┌────────────────────────────────────────────────────────────────┐
│  AxoCover로 커버리지 측정                                      │
├────────────────────────────────────────────────────────────────┤
│                                                                │
│  1. AxoCover 창에서 "Run Tests with Coverage" 클릭            │
│                                                                │
│  2. 테스트 실행 + 커버리지 수집 진행                          │
│                                                                │
│  3. 결과 표시:                                                 │
│     ┌────────────────────────────────────────────────────┐    │
│     │  Coverage Results                                   │    │
│     │                                                     │    │
│     │  CoverageDemo (27.5%)                              │    │
│     │  ├── Calculator.cs (35%)                           │    │
│     │  ├── StringUtils.cs (15%)                          │    │
│     │  └── UserService.cs (0%)                           │    │
│     │                                                     │    │
│     │  Line Coverage: 27.5%                              │    │
│     │  Branch Coverage: 18.2%                            │    │
│     └────────────────────────────────────────────────────┘    │
│                                                                │
└────────────────────────────────────────────────────────────────┘
```

### Step 18.2: 코드 하이라이트 확인

```
┌────────────────────────────────────────────────────────────────┐
│  코드 커버리지 하이라이트                                      │
├────────────────────────────────────────────────────────────────┤
│                                                                │
│  Calculator.cs를 열면 라인별 커버리지가 표시됨:               │
│                                                                │
│  ┌──────────────────────────────────────────────────────────┐ │
│  │  🟢  public int Add(int a, int b)        ← 실행됨       │ │
│  │  🟢  {                                                   │ │
│  │  🟢      return a + b;                                   │ │
│  │  🟢  }                                                   │ │
│  │                                                          │ │
│  │  🔴  public int Multiply(int a, int b)   ← 실행 안 됨   │ │
│  │  🔴  {                                                   │ │
│  │  🔴      return a * b;                                   │ │
│  │  🔴  }                                                   │ │
│  │                                                          │ │
│  │  🟢  public double Divide(int a, int b)  ← 부분 실행    │ │
│  │  🟢  {                                                   │ │
│  │  🟡      if (b == 0)                     ← 조건 일부만  │ │
│  │  🔴      {                                               │ │
│  │  🔴          throw new DivideByZero...   ← 실행 안 됨   │ │
│  │  🔴      }                                               │ │
│  │  🟢      return (double)a / b;                           │ │
│  │  🟢  }                                                   │ │
│  └──────────────────────────────────────────────────────────┘ │
│                                                                │
│  색상 의미:                                                    │
│  🟢 녹색: 실행됨 (커버됨)                                     │
│  🔴 빨간색: 실행 안 됨 (미커버)                               │
│  🟡 노란색: 부분 커버 (일부 분기만)                           │
│                                                                │
└────────────────────────────────────────────────────────────────┘
```

---

## 19. 커버리지 결과 분석

### Step 19.1: 현재 커버리지 상태

```
┌────────────────────────────────────────────────────────────────┐
│  커버리지 분석 결과                                            │
├────────────────────────────────────────────────────────────────┤
│                                                                │
│  전체 커버리지: 약 25-30%                                      │
│                                                                │
│  클래스별 분석:                                                │
│  ┌──────────────────────────────────────────────────────────┐ │
│  │ Calculator.cs (약 35%)                                   │ │
│  │ ├── Add()         ✅ 커버됨                              │ │
│  │ ├── Subtract()    ✅ 커버됨                              │ │
│  │ ├── Multiply()    ❌ 미커버 ← 테스트 없음               │ │
│  │ ├── Divide()      ⚠️ 부분 커버 ← 예외 케이스 없음       │ │
│  │ ├── Factorial()   ❌ 미커버                              │ │
│  │ ├── IsEven()      ❌ 미커버                              │ │
│  │ └── IsPrime()     ❌ 미커버                              │ │
│  └──────────────────────────────────────────────────────────┘ │
│                                                                │
│  ┌──────────────────────────────────────────────────────────┐ │
│  │ StringUtils.cs (약 15%)                                  │ │
│  │ ├── Reverse()           ⚠️ 부분 커버                     │ │
│  │ ├── IsPalindrome()      ❌ 미커버                        │ │
│  │ ├── CountWords()        ❌ 미커버                        │ │
│  │ ├── TruncateWithEllipsis() ❌ 미커버                     │ │
│  │ └── ToTitleCase()       ❌ 미커버                        │ │
│  └──────────────────────────────────────────────────────────┘ │
│                                                                │
│  ┌──────────────────────────────────────────────────────────┐ │
│  │ UserService.cs (0%)                                      │ │
│  │ └── 모든 메서드 미커버 ❌                                │ │
│  └──────────────────────────────────────────────────────────┘ │
│                                                                │
└────────────────────────────────────────────────────────────────┘
```

---

# Part 6: 커버리지 개선

---

## 20. 미커버 영역에 테스트 추가

### Step 20.1: CalculatorTests 보완

```csharp
// CoverageDemo.Tests/CalculatorTests.cs에 추가

#region Multiply 테스트 - 추가! ✅

[Test]
public void Multiply_TwoNumbers_ReturnsProduct()
{
    // Act
    var result = _calculator.Multiply(4, 5);

    // Assert
    Assert.AreEqual(20, result);
}

[Test]
public void Multiply_ByZero_ReturnsZero()
{
    // Act
    var result = _calculator.Multiply(5, 0);

    // Assert
    Assert.AreEqual(0, result);
}

#endregion

#region Divide 예외 테스트 - 추가! ✅

[Test]
public void Divide_ByZero_ThrowsException()
{
    // Act & Assert
    Assert.Throws<DivideByZeroException>(() => _calculator.Divide(10, 0));
}

#endregion

#region Factorial 테스트 - 추가! ✅

[Test]
public void Factorial_Zero_ReturnsOne()
{
    // Act
    var result = _calculator.Factorial(0);

    // Assert
    Assert.AreEqual(1, result);
}

[Test]
public void Factorial_Five_Returns120()
{
    // Act
    var result = _calculator.Factorial(5);

    // Assert
    Assert.AreEqual(120, result);
}

[Test]
public void Factorial_NegativeNumber_ThrowsException()
{
    // Act & Assert
    Assert.Throws<ArgumentException>(() => _calculator.Factorial(-1));
}

#endregion

#region IsEven 테스트 - 추가! ✅

[Test]
public void IsEven_EvenNumber_ReturnsTrue()
{
    // Act
    var result = _calculator.IsEven(4);

    // Assert
    Assert.IsTrue(result);
}

[Test]
public void IsEven_OddNumber_ReturnsFalse()
{
    // Act
    var result = _calculator.IsEven(5);

    // Assert
    Assert.IsFalse(result);
}

#endregion

#region IsPrime 테스트 - 추가! ✅

[Test]
public void IsPrime_Two_ReturnsTrue()
{
    // Act
    var result = _calculator.IsPrime(2);

    // Assert
    Assert.IsTrue(result);
}

[Test]
public void IsPrime_One_ReturnsFalse()
{
    // Act
    var result = _calculator.IsPrime(1);

    // Assert
    Assert.IsFalse(result);
}

[Test]
public void IsPrime_Seven_ReturnsTrue()
{
    // Act
    var result = _calculator.IsPrime(7);

    // Assert
    Assert.IsTrue(result);
}

[Test]
public void IsPrime_Nine_ReturnsFalse()
{
    // Act
    var result = _calculator.IsPrime(9);

    // Assert
    Assert.IsFalse(result);
}

#endregion
```

### Step 20.2: StringUtilsTests 보완

```csharp
// CoverageDemo.Tests/StringUtilsTests.cs에 추가

#region Reverse 추가 테스트

[Test]
public void Reverse_Null_ReturnsNull()
{
    // Act
    var result = _stringUtils.Reverse(null);

    // Assert
    Assert.IsNull(result);
}

[Test]
public void Reverse_Empty_ReturnsEmpty()
{
    // Act
    var result = _stringUtils.Reverse(string.Empty);

    // Assert
    Assert.AreEqual(string.Empty, result);
}

#endregion

#region IsPalindrome 테스트 - 추가! ✅

[Test]
public void IsPalindrome_ValidPalindrome_ReturnsTrue()
{
    // Act
    var result = _stringUtils.IsPalindrome("level");

    // Assert
    Assert.IsTrue(result);
}

[Test]
public void IsPalindrome_NotPalindrome_ReturnsFalse()
{
    // Act
    var result = _stringUtils.IsPalindrome("hello");

    // Assert
    Assert.IsFalse(result);
}

[Test]
public void IsPalindrome_WithSpaces_ReturnsTrue()
{
    // Act
    var result = _stringUtils.IsPalindrome("A man a plan a canal Panama");

    // Assert
    Assert.IsTrue(result);
}

#endregion

#region CountWords 테스트 - 추가! ✅

[Test]
public void CountWords_MultipleWords_ReturnsCount()
{
    // Act
    var result = _stringUtils.CountWords("Hello World Test");

    // Assert
    Assert.AreEqual(3, result);
}

[Test]
public void CountWords_Empty_ReturnsZero()
{
    // Act
    var result = _stringUtils.CountWords(string.Empty);

    // Assert
    Assert.AreEqual(0, result);
}

#endregion

#region TruncateWithEllipsis 테스트 - 추가! ✅

[Test]
public void TruncateWithEllipsis_LongString_Truncates()
{
    // Act
    var result = _stringUtils.TruncateWithEllipsis("Hello World", 8);

    // Assert
    Assert.AreEqual("Hello...", result);
}

[Test]
public void TruncateWithEllipsis_ShortString_NoChange()
{
    // Act
    var result = _stringUtils.TruncateWithEllipsis("Hi", 10);

    // Assert
    Assert.AreEqual("Hi", result);
}

[Test]
public void TruncateWithEllipsis_MaxLengthTooSmall_ThrowsException()
{
    // Act & Assert
    Assert.Throws<ArgumentException>(() => 
        _stringUtils.TruncateWithEllipsis("Hello", 2));
}

#endregion

#region ToTitleCase 테스트 - 추가! ✅

[Test]
public void ToTitleCase_LowerCase_ReturnsProperCase()
{
    // Act
    var result = _stringUtils.ToTitleCase("hello world");

    // Assert
    Assert.AreEqual("Hello World", result);
}

[Test]
public void ToTitleCase_Empty_ReturnsEmpty()
{
    // Act
    var result = _stringUtils.ToTitleCase(string.Empty);

    // Assert
    Assert.AreEqual(string.Empty, result);
}

#endregion
```

### Step 20.3: UserServiceTests 작성

```
┌────────────────────────────────────────────────────────────────┐
│  UserServiceTests.cs 추가                                      │
├────────────────────────────────────────────────────────────────┤
│                                                                │
│  1. CoverageDemo.Tests 프로젝트 우클릭                         │
│                                                                │
│  2. 추가 → 클래스                                             │
│                                                                │
│  3. 이름: UserServiceTests.cs                                  │
│                                                                │
│  4. "추가" 클릭                                                │
│                                                                │
└────────────────────────────────────────────────────────────────┘
```

```csharp
// CoverageDemo.Tests/UserServiceTests.cs
using System;
using System.Linq;
using NUnit.Framework;

namespace CoverageDemo.Tests
{
    /// <summary>
    /// UserService 테스트 클래스
    /// </summary>
    [TestFixture]
    public class UserServiceTests
    {
        private UserService _userService;

        [SetUp]
        public void SetUp()
        {
            _userService = new UserService();
        }

        #region Add 테스트

        [Test]
        public void Add_ValidUser_AddsSuccessfully()
        {
            // Arrange
            var user = new User
            {
                Name = "홍길동",
                Email = "hong@test.com",
                Age = 30
            };

            // Act
            _userService.Add(user);

            // Assert
            var result = _userService.GetById(1);
            Assert.IsNotNull(result);
            Assert.AreEqual("홍길동", result.Name);
        }

        [Test]
        public void Add_NullUser_ThrowsException()
        {
            // Act & Assert
            Assert.Throws<ArgumentNullException>(() => _userService.Add(null));
        }

        [Test]
        public void Add_EmptyName_ThrowsException()
        {
            // Arrange
            var user = new User { Name = "", Email = "test@test.com" };

            // Act & Assert
            Assert.Throws<ArgumentException>(() => _userService.Add(user));
        }

        [Test]
        public void Add_InvalidEmail_ThrowsException()
        {
            // Arrange
            var user = new User { Name = "Test", Email = "invalid-email" };

            // Act & Assert
            Assert.Throws<ArgumentException>(() => _userService.Add(user));
        }

        [Test]
        public void Add_DuplicateEmail_ThrowsException()
        {
            // Arrange
            var user1 = new User { Name = "User1", Email = "same@test.com" };
            var user2 = new User { Name = "User2", Email = "same@test.com" };

            _userService.Add(user1);

            // Act & Assert
            Assert.Throws<InvalidOperationException>(() => _userService.Add(user2));
        }

        #endregion

        #region GetAll / GetActiveUsers / GetAdultUsers 테스트

        [Test]
        public void GetAll_MultipleUsers_ReturnsAll()
        {
            // Arrange
            _userService.Add(new User { Name = "User1", Email = "u1@test.com" });
            _userService.Add(new User { Name = "User2", Email = "u2@test.com" });

            // Act
            var result = _userService.GetAll().ToList();

            // Assert
            Assert.AreEqual(2, result.Count);
        }

        [Test]
        public void GetActiveUsers_MixedUsers_ReturnsOnlyActive()
        {
            // Arrange
            _userService.Add(new User { Name = "Active", Email = "a@test.com", IsActive = true });
            _userService.Add(new User { Name = "Inactive", Email = "i@test.com", IsActive = false });

            // Act
            var result = _userService.GetActiveUsers().ToList();

            // Assert
            Assert.AreEqual(1, result.Count);
            Assert.AreEqual("Active", result[0].Name);
        }

        [Test]
        public void GetAdultUsers_MixedAges_ReturnsOnlyAdults()
        {
            // Arrange
            _userService.Add(new User { Name = "Adult", Email = "adult@test.com", Age = 25 });
            _userService.Add(new User { Name = "Minor", Email = "minor@test.com", Age = 15 });

            // Act
            var result = _userService.GetAdultUsers().ToList();

            // Assert
            Assert.AreEqual(1, result.Count);
            Assert.AreEqual("Adult", result[0].Name);
        }

        #endregion

        #region Delete 테스트

        [Test]
        public void Delete_ExistingUser_ReturnsTrue()
        {
            // Arrange
            _userService.Add(new User { Name = "ToDelete", Email = "del@test.com" });

            // Act
            var result = _userService.Delete(1);

            // Assert
            Assert.IsTrue(result);
            Assert.IsNull(_userService.GetById(1));
        }

        [Test]
        public void Delete_NonExistingUser_ReturnsFalse()
        {
            // Act
            var result = _userService.Delete(999);

            // Assert
            Assert.IsFalse(result);
        }

        #endregion

        #region Deactivate 테스트

        [Test]
        public void Deactivate_ExistingUser_SetsInactive()
        {
            // Arrange
            _userService.Add(new User { Name = "Test", Email = "test@test.com", IsActive = true });

            // Act
            _userService.Deactivate(1);

            // Assert
            var user = _userService.GetById(1);
            Assert.IsFalse(user.IsActive);
        }

        [Test]
        public void Deactivate_NonExistingUser_ThrowsException()
        {
            // Act & Assert
            Assert.Throws<InvalidOperationException>(() => _userService.Deactivate(999));
        }

        #endregion
    }
}
```

---

## 21. 커버리지 재측정

### Step 21.1: 다시 테스트 실행

```
┌────────────────────────────────────────────────────────────────┐
│  커버리지 재측정                                               │
├────────────────────────────────────────────────────────────────┤
│                                                                │
│  1. 솔루션 빌드 (Ctrl + Shift + B)                            │
│                                                                │
│  2. AxoCover 창에서 "Run Tests with Coverage" 클릭            │
│     또는 배치 파일 실행                                       │
│                                                                │
│  3. 결과 확인                                                  │
│                                                                │
└────────────────────────────────────────────────────────────────┘
```

### Step 21.2: 개선된 커버리지 확인

```
┌────────────────────────────────────────────────────────────────┐
│  개선된 커버리지 결과                                          │
├────────────────────────────────────────────────────────────────┤
│                                                                │
│  이전 → 이후                                                   │
│                                                                │
│  ┌──────────────────────────────────────────────────────────┐ │
│  │ 클래스           │ 이전    │ 이후    │ 변화             │ │
│  ├──────────────────┼─────────┼─────────┼──────────────────┤ │
│  │ Calculator.cs    │ 35%     │ 95%     │ +60% ⬆️          │ │
│  │ StringUtils.cs   │ 15%     │ 90%     │ +75% ⬆️          │ │
│  │ UserService.cs   │ 0%      │ 85%     │ +85% ⬆️          │ │
│  ├──────────────────┼─────────┼─────────┼──────────────────┤ │
│  │ 전체             │ 27%     │ 90%     │ +63% ⬆️          │ │
│  └──────────────────────────────────────────────────────────┘ │
│                                                                │
│  ✅ 목표 달성! (80% 이상)                                      │
│                                                                │
└────────────────────────────────────────────────────────────────┘
```

---

# Part 7: 정리 및 베스트 프랙티스

---

## 22. 커버리지 베스트 프랙티스

### Step 22.1: 권장 사항

```
┌────────────────────────────────────────────────────────────────┐
│  코드 커버리지 베스트 프랙티스                                 │
├────────────────────────────────────────────────────────────────┤
│                                                                │
│  1. 목표 설정                                                  │
│     • 신규 코드: 80% 이상                                     │
│     • 핵심 비즈니스 로직: 90% 이상                            │
│     • 전체 프로젝트: 70% 이상                                 │
│                                                                │
│  2. 품질 중심                                                  │
│     • 숫자보다 테스트 품질 중시                               │
│     • 의미 있는 Assert 포함                                   │
│     • 경계값/예외 케이스 테스트                               │
│                                                                │
│  3. 정기적 측정                                                │
│     • 커밋/PR마다 커버리지 체크                               │
│     • 커버리지 하락 방지                                      │
│     • 팀 문화로 정착                                          │
│                                                                │
│  4. 제외 항목                                                  │
│     • 자동 생성 코드                                          │
│     • 단순 DTO/모델 클래스                                    │
│     • UI 관련 코드 (WPF, WinForms)                            │
│                                                                │
└────────────────────────────────────────────────────────────────┘
```

---

## 23. 핵심 요약

### Step 23.1: 오늘 배운 내용

```
┌────────────────────────────────────────────────────────────────┐
│  📊 핵심 요약                                                  │
├────────────────────────────────────────────────────────────────┤
│                                                                │
│  도구 조합 (VS 2017):                                          │
│  • AxoCover (Visual Studio 확장) - GUI에서 바로 측정          │
│  • 또는 OpenCover + ReportGenerator (배치 파일)               │
│                                                                │
│  커버리지 종류:                                                │
│  • 라인 커버리지: 실행된 코드 라인 비율                       │
│  • 브랜치 커버리지: 실행된 분기 비율                          │
│  • 메서드 커버리지: 호출된 메서드 비율                        │
│                                                                │
│  권장 목표:                                                    │
│  • 신규 코드: 80% 이상                                        │
│  • 전체: 70% 이상                                             │
│  • 숫자보다 테스트 품질!                                      │
│                                                                │
│  핵심 포인트:                                                  │
│  • 높은 커버리지 ≠ 좋은 테스트                                │
│  • Assert 없이 실행만 해도 커버리지는 올라감                  │
│  • 경계값, 예외 케이스 테스트가 중요                          │
│                                                                │
└────────────────────────────────────────────────────────────────┘
```

---

## 24. 최종 프로젝트 구조

### Step 24.1: 완성된 솔루션 구조

```
┌────────────────────────────────────────────────────────────────┐
│  최종 솔루션 구조                                              │
├────────────────────────────────────────────────────────────────┤
│                                                                │
│  솔루션 'CoverageDemo'                                         │
│  │                                                             │
│  ├── CoverageDemo                    [프로덕션 코드]           │
│  │   ├── Calculator.cs               (7개 메서드)              │
│  │   ├── StringUtils.cs              (5개 메서드)              │
│  │   └── UserService.cs              (8개 메서드)              │
│  │                                                             │
│  ├── CoverageDemo.Tests              [테스트 코드]             │
│  │   ├── CalculatorTests.cs          (16개 테스트)             │
│  │   ├── StringUtilsTests.cs         (12개 테스트)             │
│  │   └── UserServiceTests.cs         (12개 테스트)             │
│  │                                                             │
│  └── (RunCoverage.bat)               [선택: 배치 파일]         │
│                                                                │
│  총 테스트: 40개                                               │
│  커버리지: 약 90%                                              │
│                                                                │
└────────────────────────────────────────────────────────────────┘
```

---

# 🎉 축하합니다!

코드 커버리지 측정 실습을 완료했습니다!

이제 여러분은:
- **Visual Studio 2017**에서 코드 커버리지를 측정할 수 있습니다
- **AxoCover**로 GUI에서 바로 커버리지를 확인할 수 있습니다
- 커버리지 결과를 분석하고 **테스트를 개선**할 수 있습니다
- **베스트 프랙티스**에 따라 커버리지를 관리할 수 있습니다

프로젝트에 커버리지 측정을 적용하여 코드 품질을 높이세요!

---

**수고하셨습니다! 🎉**
