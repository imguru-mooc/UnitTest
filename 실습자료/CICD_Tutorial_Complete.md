# 🚀 C# CI/CD 통합 따라하기 실습 (상세 버전)

## GitHub Actions로 자동 테스트, PR 시 테스트 자동 실행

> **실습 환경**: GitHub + .NET 8.0  
> **도구**: GitHub Actions, NUnit, Coverlet  
> **예상 소요 시간**: 약 120분

---

## 📋 목차

- Part 1: CI/CD 개념 이해 (Step 1-10)
- Part 2: GitHub Actions 기초 (Step 11-20)
- Part 3: .NET 프로젝트 CI 구성 (Step 21-32)
- Part 4: PR 자동 테스트 (Step 33-42)
- Part 5: 코드 품질 체크 (Step 43-52)
- Part 6: 고급 워크플로우 (Step 53-62)
- Part 7: 실전 시나리오 (Step 63-72)
- Part 8: 정리 및 베스트 프랙티스 (Step 73-80)

---

# Part 1: CI/CD 개념 이해

---

## 1. CI/CD란?

### Step 1.1: CI (Continuous Integration)

```
┌────────────────────────────────────────────────────────────────┐
│  CI - 지속적 통합 (Continuous Integration)                     │
├────────────────────────────────────────────────────────────────┤
│                                                                │
│  개발자가 코드를 자주 메인 브랜치에 통합하고,                  │
│  자동으로 빌드와 테스트를 실행하는 방식                        │
│                                                                │
│  ┌─────────────────────────────────────────────────────────┐  │
│  │                                                         │  │
│  │   개발자 A ──┐                                          │  │
│  │              │     Push                                 │  │
│  │   개발자 B ──┼────────►  GitHub  ────►  자동 빌드       │  │
│  │              │           Repository      & 테스트       │  │
│  │   개발자 C ──┘                              │           │  │
│  │                                             │           │  │
│  │                                             ▼           │  │
│  │                                      ✅ 성공 / ❌ 실패  │  │
│  │                                                         │  │
│  └─────────────────────────────────────────────────────────┘  │
│                                                                │
│  핵심 원칙:                                                    │
│  • 자주 통합 (하루에 여러 번)                                 │
│  • 자동 빌드                                                  │
│  • 자동 테스트                                                │
│  • 빠른 피드백                                                │
│                                                                │
└────────────────────────────────────────────────────────────────┘
```

### Step 1.2: CD (Continuous Delivery/Deployment)

```
┌────────────────────────────────────────────────────────────────┐
│  CD - 지속적 전달/배포                                         │
├────────────────────────────────────────────────────────────────┤
│                                                                │
│  Continuous Delivery (지속적 전달):                            │
│  • 언제든 배포 가능한 상태 유지                               │
│  • 배포는 수동 승인 후 진행                                   │
│                                                                │
│  Continuous Deployment (지속적 배포):                          │
│  • 테스트 통과 시 자동으로 프로덕션 배포                      │
│  • 완전 자동화                                                │
│                                                                │
│  ┌─────────────────────────────────────────────────────────┐  │
│  │                                                         │  │
│  │  Code → Build → Test → Stage → Deploy                  │  │
│  │    │      │       │       │       │                     │  │
│  │    │      │       │       │       │                     │  │
│  │    └──────┴───────┴───────┴───────┘                     │  │
│  │              CI              CD                         │  │
│  │                                                         │  │
│  └─────────────────────────────────────────────────────────┘  │
│                                                                │
└────────────────────────────────────────────────────────────────┘
```

---

## 2. CI/CD의 장점

### Step 2.1: 주요 이점

```
┌────────────────────────────────────────────────────────────────┐
│  CI/CD의 장점                                                  │
├────────────────────────────────────────────────────────────────┤
│                                                                │
│  1. 빠른 피드백                                                │
│  ───────────────────────────────────────────────────────────  │
│  • 코드 푸시 후 몇 분 내에 결과 확인                          │
│  • 버그 조기 발견                                             │
│  • 문제 해결 비용 감소                                        │
│                                                                │
│  2. 품질 향상                                                  │
│  ───────────────────────────────────────────────────────────  │
│  • 모든 코드 변경에 테스트 실행                               │
│  • 회귀 버그 방지                                             │
│  • 코드 품질 일관성 유지                                      │
│                                                                │
│  3. 개발 속도 향상                                             │
│  ───────────────────────────────────────────────────────────  │
│  • 수동 작업 자동화                                           │
│  • 반복 작업 제거                                             │
│  • 개발자가 코딩에 집중                                       │
│                                                                │
│  4. 위험 감소                                                  │
│  ───────────────────────────────────────────────────────────  │
│  • 작은 단위로 자주 배포                                      │
│  • 문제 발생 시 빠른 롤백                                     │
│  • 대규모 릴리스 위험 제거                                    │
│                                                                │
│  5. 팀 협업 개선                                               │
│  ───────────────────────────────────────────────────────────  │
│  • 통합 충돌 조기 발견                                        │
│  • 코드 리뷰 효율화                                           │
│  • 투명한 빌드 상태                                           │
│                                                                │
└────────────────────────────────────────────────────────────────┘
```

---

## 3. CI/CD 파이프라인

### Step 3.1: 일반적인 파이프라인 구조

```
┌────────────────────────────────────────────────────────────────┐
│  CI/CD 파이프라인 구조                                         │
├────────────────────────────────────────────────────────────────┤
│                                                                │
│  ┌──────────────────────────────────────────────────────────┐ │
│  │                                                          │ │
│  │  1. Source     2. Build      3. Test       4. Deploy    │ │
│  │  ┌───────┐    ┌───────┐    ┌───────┐    ┌───────┐       │ │
│  │  │ Code  │───►│ Build │───►│ Test  │───►│Deploy │       │ │
│  │  │ Push  │    │       │    │       │    │       │       │ │
│  │  └───────┘    └───────┘    └───────┘    └───────┘       │ │
│  │      │            │            │            │            │ │
│  │      ▼            ▼            ▼            ▼            │ │
│  │  - Git Push   - Restore    - Unit Test  - Staging       │ │
│  │  - PR 생성    - Compile    - 통합 Test  - Production    │ │
│  │  - 태그       - Publish    - 커버리지                   │ │
│  │                            - 정적분석                    │ │
│  │                                                          │ │
│  └──────────────────────────────────────────────────────────┘ │
│                                                                │
│  각 단계에서 실패하면 다음 단계로 진행하지 않음!               │
│                                                                │
└────────────────────────────────────────────────────────────────┘
```

---

## 4. GitHub Actions 소개

### Step 4.1: GitHub Actions란?

```
┌────────────────────────────────────────────────────────────────┐
│  GitHub Actions                                                │
├────────────────────────────────────────────────────────────────┤
│                                                                │
│  GitHub에서 제공하는 CI/CD 플랫폼                              │
│                                                                │
│  특징:                                                         │
│  • GitHub 저장소에 내장                                       │
│  • YAML로 워크플로우 정의                                     │
│  • 무료 티어 제공 (Public 저장소)                             │
│  • 풍부한 마켓플레이스 (재사용 가능한 액션)                   │
│  • 다양한 OS 지원 (Linux, Windows, macOS)                     │
│                                                                │
│  무료 사용량 (Public 저장소):                                  │
│  • 무제한                                                     │
│                                                                │
│  무료 사용량 (Private 저장소):                                 │
│  • 2,000분/월 (Free 플랜)                                     │
│  • 3,000분/월 (Pro 플랜)                                      │
│                                                                │
└────────────────────────────────────────────────────────────────┘
```

---

## 5. 핵심 용어

### Step 5.1: GitHub Actions 용어

```
┌────────────────────────────────────────────────────────────────┐
│  GitHub Actions 핵심 용어                                      │
├────────────────────────────────────────────────────────────────┤
│                                                                │
│  Workflow (워크플로우)                                         │
│  ─────────────────────────────────────────────────────────────│
│  • 자동화된 프로세스 전체                                     │
│  • YAML 파일로 정의                                           │
│  • .github/workflows/ 디렉토리에 저장                         │
│                                                                │
│  Event (이벤트)                                                │
│  ─────────────────────────────────────────────────────────────│
│  • 워크플로우를 트리거하는 활동                               │
│  • 예: push, pull_request, schedule                           │
│                                                                │
│  Job (작업)                                                    │
│  ─────────────────────────────────────────────────────────────│
│  • 같은 러너에서 실행되는 스텝들의 집합                       │
│  • 병렬 또는 순차 실행 가능                                   │
│                                                                │
│  Step (스텝)                                                   │
│  ─────────────────────────────────────────────────────────────│
│  • 작업 내의 개별 태스크                                      │
│  • 명령어 실행 또는 액션 사용                                 │
│                                                                │
│  Action (액션)                                                 │
│  ─────────────────────────────────────────────────────────────│
│  • 재사용 가능한 워크플로우 단위                              │
│  • 마켓플레이스에서 공유                                      │
│                                                                │
│  Runner (러너)                                                 │
│  ─────────────────────────────────────────────────────────────│
│  • 워크플로우를 실행하는 서버                                 │
│  • GitHub 호스팅 또는 셀프 호스팅                             │
│                                                                │
└────────────────────────────────────────────────────────────────┘
```

---

## 6. 워크플로우 구조

### Step 6.1: YAML 구조 개요

```yaml
# 워크플로우 구조 예시

name: 워크플로우 이름        # 워크플로우 이름

on:                          # 이벤트 (트리거)
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]

jobs:                        # 작업들
  build:                     # 작업 이름
    runs-on: ubuntu-latest   # 러너 지정
    
    steps:                   # 스텝들
    - name: Checkout         # 스텝 이름
      uses: actions/checkout@v4  # 액션 사용
    
    - name: Run command      # 스텝 이름
      run: echo "Hello"      # 명령어 실행
```

### Step 6.2: 구조 시각화

```
┌────────────────────────────────────────────────────────────────┐
│  워크플로우 구조 시각화                                        │
├────────────────────────────────────────────────────────────────┤
│                                                                │
│  Workflow: CI Pipeline                                         │
│  ┌──────────────────────────────────────────────────────────┐ │
│  │                                                          │ │
│  │  Event: push to main                                     │ │
│  │           │                                              │ │
│  │           ▼                                              │ │
│  │  ┌─────────────────────────────────────────────────────┐│ │
│  │  │ Job: build                                          ││ │
│  │  │ Runner: ubuntu-latest                               ││ │
│  │  │ ┌─────────────────────────────────────────────────┐ ││ │
│  │  │ │ Step 1: Checkout                                │ ││ │
│  │  │ │         uses: actions/checkout@v4               │ ││ │
│  │  │ ├─────────────────────────────────────────────────┤ ││ │
│  │  │ │ Step 2: Setup .NET                              │ ││ │
│  │  │ │         uses: actions/setup-dotnet@v3           │ ││ │
│  │  │ ├─────────────────────────────────────────────────┤ ││ │
│  │  │ │ Step 3: Build                                   │ ││ │
│  │  │ │         run: dotnet build                       │ ││ │
│  │  │ ├─────────────────────────────────────────────────┤ ││ │
│  │  │ │ Step 4: Test                                    │ ││ │
│  │  │ │         run: dotnet test                        │ ││ │
│  │  │ └─────────────────────────────────────────────────┘ ││ │
│  │  └─────────────────────────────────────────────────────┘│ │
│  │                                                          │ │
│  └──────────────────────────────────────────────────────────┘ │
│                                                                │
└────────────────────────────────────────────────────────────────┘
```

---

## 7. 이벤트 종류

### Step 7.1: 주요 이벤트

```
┌────────────────────────────────────────────────────────────────┐
│  주요 GitHub Actions 이벤트                                    │
├────────────────────────────────────────────────────────────────┤
│                                                                │
│  push                                                          │
│  ─────────────────────────────────────────────────────────────│
│  • 브랜치에 푸시할 때 트리거                                  │
│  on:                                                          │
│    push:                                                      │
│      branches: [ main, develop ]                              │
│                                                                │
│  pull_request                                                  │
│  ─────────────────────────────────────────────────────────────│
│  • PR 생성/업데이트 시 트리거                                 │
│  on:                                                          │
│    pull_request:                                              │
│      branches: [ main ]                                       │
│      types: [ opened, synchronize, reopened ]                 │
│                                                                │
│  schedule                                                      │
│  ─────────────────────────────────────────────────────────────│
│  • 크론 스케줄로 주기적 실행                                  │
│  on:                                                          │
│    schedule:                                                  │
│      - cron: '0 0 * * *'  # 매일 자정                        │
│                                                                │
│  workflow_dispatch                                             │
│  ─────────────────────────────────────────────────────────────│
│  • 수동 실행                                                  │
│  on:                                                          │
│    workflow_dispatch:                                         │
│                                                                │
│  release                                                       │
│  ─────────────────────────────────────────────────────────────│
│  • 릴리스 생성 시 트리거                                      │
│  on:                                                          │
│    release:                                                   │
│      types: [ published ]                                     │
│                                                                │
└────────────────────────────────────────────────────────────────┘
```

---

## 8. 러너 종류

### Step 8.1: GitHub 호스팅 러너

```
┌────────────────────────────────────────────────────────────────┐
│  GitHub 호스팅 러너                                            │
├────────────────────────────────────────────────────────────────┤
│                                                                │
│  Linux:                                                        │
│  • ubuntu-latest (Ubuntu 22.04)                               │
│  • ubuntu-22.04                                               │
│  • ubuntu-20.04                                               │
│                                                                │
│  Windows:                                                      │
│  • windows-latest (Windows Server 2022)                       │
│  • windows-2022                                               │
│  • windows-2019                                               │
│                                                                │
│  macOS:                                                        │
│  • macos-latest (macOS 14)                                    │
│  • macos-14                                                   │
│  • macos-13                                                   │
│  • macos-12                                                   │
│                                                                │
│  사용 방법:                                                    │
│  jobs:                                                        │
│    build:                                                     │
│      runs-on: ubuntu-latest                                   │
│                                                                │
└────────────────────────────────────────────────────────────────┘
```

---

## 9. 사전 준비

### Step 9.1: 필요한 것

```
┌────────────────────────────────────────────────────────────────┐
│  실습 사전 준비                                                │
├────────────────────────────────────────────────────────────────┤
│                                                                │
│  필수:                                                         │
│  ☐ GitHub 계정                                                │
│  ☐ Git 설치                                                   │
│  ☐ .NET SDK 8.0 설치                                          │
│  ☐ 테스트가 있는 .NET 프로젝트                                │
│                                                                │
│  선택:                                                         │
│  ☐ Visual Studio 또는 VS Code                                 │
│  ☐ GitHub CLI (gh)                                            │
│                                                                │
│  확인 명령어:                                                  │
│  > git --version                                              │
│  > dotnet --version                                           │
│  > gh --version (선택)                                        │
│                                                                │
└────────────────────────────────────────────────────────────────┘
```

---

## 10. TODO 리스트

### Step 10.1: 실습 목표

```
┌────────────────────────────────────────────────────────────────┐
│  📝 실습 TODO 리스트                                           │
├────────────────────────────────────────────────────────────────┤
│                                                                │
│  기본 CI 구성                                                  │
│  [ ] GitHub 저장소 생성                                       │
│  [ ] .NET 프로젝트 푸시                                       │
│  [ ] 기본 워크플로우 생성                                     │
│  [ ] 빌드 자동화                                              │
│  [ ] 테스트 자동화                                            │
│                                                                │
│  PR 자동 테스트                                                │
│  [ ] PR 트리거 설정                                           │
│  [ ] 상태 체크 설정                                           │
│  [ ] 브랜치 보호 규칙                                         │
│                                                                │
│  코드 품질                                                     │
│  [ ] 커버리지 측정                                            │
│  [ ] 커버리지 리포트                                          │
│  [ ] 린트 체크                                                │
│                                                                │
│  고급 기능                                                     │
│  [ ] 매트릭스 빌드                                            │
│  [ ] 캐싱                                                     │
│  [ ] 아티팩트                                                 │
│  [ ] 환경 변수 / 시크릿                                       │
│                                                                │
└────────────────────────────────────────────────────────────────┘
```

---

# Part 2: GitHub Actions 기초

---

## 11. 샘플 프로젝트 준비

### Step 11.1: 프로젝트 구조

```
┌────────────────────────────────────────────────────────────────┐
│  샘플 프로젝트 구조                                            │
├────────────────────────────────────────────────────────────────┤
│                                                                │
│  MyCalculator/                                                 │
│  │                                                             │
│  ├── src/                                                      │
│  │   └── MyCalculator/                                         │
│  │       ├── Calculator.cs                                     │
│  │       └── MyCalculator.csproj                               │
│  │                                                             │
│  ├── tests/                                                    │
│  │   └── MyCalculator.Tests/                                   │
│  │       ├── CalculatorTests.cs                                │
│  │       └── MyCalculator.Tests.csproj                         │
│  │                                                             │
│  ├── .github/                                                  │
│  │   └── workflows/                                            │
│  │       └── ci.yml                    ← 워크플로우 파일      │
│  │                                                             │
│  ├── .gitignore                                                │
│  ├── MyCalculator.sln                                          │
│  └── README.md                                                 │
│                                                                │
└────────────────────────────────────────────────────────────────┘
```

---

## 12. 프로젝트 생성

### Step 12.1: 프로젝트 생성 명령어

```bash
# 디렉토리 생성
mkdir MyCalculator
cd MyCalculator

# 솔루션 생성
dotnet new sln -n MyCalculator

# 프로덕션 프로젝트 생성
mkdir -p src/MyCalculator
dotnet new classlib -n MyCalculator -o src/MyCalculator -f net8.0

# 테스트 프로젝트 생성
mkdir -p tests/MyCalculator.Tests
dotnet new nunit -n MyCalculator.Tests -o tests/MyCalculator.Tests -f net8.0

# 솔루션에 프로젝트 추가
dotnet sln add src/MyCalculator/MyCalculator.csproj
dotnet sln add tests/MyCalculator.Tests/MyCalculator.Tests.csproj

# 테스트 프로젝트에서 프로덕션 프로젝트 참조
dotnet add tests/MyCalculator.Tests/MyCalculator.Tests.csproj reference src/MyCalculator/MyCalculator.csproj

# 기본 파일 삭제
rm src/MyCalculator/Class1.cs
rm tests/MyCalculator.Tests/UnitTest1.cs
```

---

## 13. 프로덕션 코드 작성

### Step 13.1: Calculator.cs

```csharp
// src/MyCalculator/Calculator.cs
namespace MyCalculator
{
    public class Calculator
    {
        public int Add(int a, int b)
        {
            return a + b;
        }
        
        public int Subtract(int a, int b)
        {
            return a - b;
        }
        
        public int Multiply(int a, int b)
        {
            return a * b;
        }
        
        public double Divide(int a, int b)
        {
            if (b == 0)
            {
                throw new DivideByZeroException("0으로 나눌 수 없습니다.");
            }
            return (double)a / b;
        }
        
        public bool IsEven(int number)
        {
            return number % 2 == 0;
        }
    }
}
```

---

## 14. 테스트 코드 작성

### Step 14.1: CalculatorTests.cs

```csharp
// tests/MyCalculator.Tests/CalculatorTests.cs
using NUnit.Framework;

namespace MyCalculator.Tests
{
    [TestFixture]
    public class CalculatorTests
    {
        private Calculator _calculator;
        
        [SetUp]
        public void SetUp()
        {
            _calculator = new Calculator();
        }
        
        [TestCase(2, 3, 5)]
        [TestCase(-1, 1, 0)]
        [TestCase(0, 0, 0)]
        public void Add_TwoNumbers_ReturnsSum(int a, int b, int expected)
        {
            var result = _calculator.Add(a, b);
            Assert.AreEqual(expected, result);
        }
        
        [TestCase(5, 3, 2)]
        [TestCase(3, 5, -2)]
        public void Subtract_TwoNumbers_ReturnsDifference(int a, int b, int expected)
        {
            var result = _calculator.Subtract(a, b);
            Assert.AreEqual(expected, result);
        }
        
        [TestCase(2, 3, 6)]
        [TestCase(0, 5, 0)]
        [TestCase(-2, 3, -6)]
        public void Multiply_TwoNumbers_ReturnsProduct(int a, int b, int expected)
        {
            var result = _calculator.Multiply(a, b);
            Assert.AreEqual(expected, result);
        }
        
        [Test]
        public void Divide_ByZero_ThrowsException()
        {
            Assert.Throws<DivideByZeroException>(() => 
                _calculator.Divide(10, 0));
        }
        
        [TestCase(10, 2, 5.0)]
        [TestCase(7, 2, 3.5)]
        public void Divide_TwoNumbers_ReturnsQuotient(int a, int b, double expected)
        {
            var result = _calculator.Divide(a, b);
            Assert.AreEqual(expected, result);
        }
        
        [TestCase(2, true)]
        [TestCase(3, false)]
        [TestCase(0, true)]
        [TestCase(-4, true)]
        public void IsEven_Number_ReturnsCorrectResult(int number, bool expected)
        {
            var result = _calculator.IsEven(number);
            Assert.AreEqual(expected, result);
        }
    }
}
```

---

## 15. 로컬에서 테스트 확인

### Step 15.1: 빌드 및 테스트

```bash
# 빌드
dotnet build

# 테스트 실행
dotnet test

# 결과 확인
# Passed!  - Failed: 0, Passed: 12, Skipped: 0
```

---

## 16. Git 초기화 및 .gitignore

### Step 16.1: .gitignore 생성

```bash
# .gitignore 생성
cat > .gitignore << 'EOF'
## .NET
bin/
obj/
*.user
*.userosscache
*.suo
*.cache

## Visual Studio
.vs/
*.sln.docstates

## Test Results
TestResults/
coverage/

## IDE
.idea/
*.swp
.vscode/
!.vscode/settings.json
!.vscode/tasks.json
!.vscode/launch.json

## OS
.DS_Store
Thumbs.db
EOF
```

### Step 16.2: Git 초기화

```bash
# Git 초기화
git init

# 파일 추가
git add .

# 첫 커밋
git commit -m "Initial commit: Calculator project with tests"
```

---

## 17. GitHub 저장소 생성

### Step 17.1: GitHub에서 저장소 생성

```
┌────────────────────────────────────────────────────────────────┐
│  GitHub 저장소 생성                                            │
├────────────────────────────────────────────────────────────────┤
│                                                                │
│  방법 1: GitHub 웹사이트                                       │
│  1. github.com 로그인                                         │
│  2. "New repository" 클릭                                     │
│  3. 이름: MyCalculator                                        │
│  4. Public 또는 Private 선택                                  │
│  5. "Create repository" 클릭                                  │
│                                                                │
│  방법 2: GitHub CLI                                            │
│  > gh repo create MyCalculator --public --source=.            │
│                                                                │
└────────────────────────────────────────────────────────────────┘
```

### Step 17.2: 원격 저장소 연결 및 푸시

```bash
# 원격 저장소 추가
git remote add origin https://github.com/YOUR_USERNAME/MyCalculator.git

# 푸시
git branch -M main
git push -u origin main
```

---

## 18. 워크플로우 디렉토리 생성

### Step 18.1: .github/workflows 디렉토리

```bash
# 워크플로우 디렉토리 생성
mkdir -p .github/workflows
```

```
┌────────────────────────────────────────────────────────────────┐
│  워크플로우 파일 위치                                          │
├────────────────────────────────────────────────────────────────┤
│                                                                │
│  중요: 워크플로우 파일은 반드시                                │
│        .github/workflows/ 디렉토리에 있어야 함!               │
│                                                                │
│  MyCalculator/                                                 │
│  └── .github/                                                  │
│      └── workflows/         ← 이 디렉토리!                    │
│          ├── ci.yml                                            │
│          ├── pr-check.yml                                      │
│          └── release.yml                                       │
│                                                                │
│  파일 형식: YAML (.yml 또는 .yaml)                            │
│                                                                │
└────────────────────────────────────────────────────────────────┘
```

---

## 19. 첫 번째 워크플로우 생성

### Step 19.1: ci.yml 생성

```yaml
# .github/workflows/ci.yml
name: CI

on:
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]

jobs:
  build:
    runs-on: ubuntu-latest
    
    steps:
    - name: Checkout code
      uses: actions/checkout@v4
    
    - name: Setup .NET
      uses: actions/setup-dotnet@v4
      with:
        dotnet-version: '8.0.x'
    
    - name: Restore dependencies
      run: dotnet restore
    
    - name: Build
      run: dotnet build --no-restore
    
    - name: Test
      run: dotnet test --no-build --verbosity normal
```

---

## 20. 워크플로우 푸시 및 확인

### Step 20.1: 변경사항 푸시

```bash
# 워크플로우 파일 추가
git add .github/workflows/ci.yml

# 커밋
git commit -m "Add CI workflow"

# 푸시
git push
```

### Step 20.2: GitHub에서 확인

```
┌────────────────────────────────────────────────────────────────┐
│  GitHub Actions 탭에서 확인                                    │
├────────────────────────────────────────────────────────────────┤
│                                                                │
│  1. GitHub 저장소 페이지로 이동                               │
│  2. "Actions" 탭 클릭                                         │
│  3. 워크플로우 실행 확인                                      │
│                                                                │
│  ┌──────────────────────────────────────────────────────────┐ │
│  │  Actions                                                 │ │
│  ├──────────────────────────────────────────────────────────┤ │
│  │                                                          │ │
│  │  ✅ CI                                                   │ │
│  │     Add CI workflow - main                              │ │
│  │     completed 30 seconds ago                            │ │
│  │                                                          │ │
│  │     • build  ✅ 45s                                     │ │
│  │                                                          │ │
│  └──────────────────────────────────────────────────────────┘ │
│                                                                │
│  워크플로우 이름을 클릭하면 상세 로그 확인 가능                │
│                                                                │
└────────────────────────────────────────────────────────────────┘
```

---

# Part 3: .NET 프로젝트 CI 구성

---

## 21. 워크플로우 상세 분석

### Step 21.1: 각 부분 설명

```yaml
# 워크플로우 이름 (GitHub UI에 표시)
name: CI

# 트리거 설정
on:
  push:
    branches: [ main ]      # main 브랜치에 push 시
  pull_request:
    branches: [ main ]      # main 브랜치로 PR 생성 시

# 작업 정의
jobs:
  build:                    # 작업 ID (고유해야 함)
    runs-on: ubuntu-latest  # 실행 환경
    
    steps:
    # 1. 코드 체크아웃
    - name: Checkout code
      uses: actions/checkout@v4
      # GitHub 저장소의 코드를 러너에 복사
    
    # 2. .NET SDK 설정
    - name: Setup .NET
      uses: actions/setup-dotnet@v4
      with:
        dotnet-version: '8.0.x'
      # .NET 8.0.x 버전 설치
    
    # 3. 패키지 복원
    - name: Restore dependencies
      run: dotnet restore
      # NuGet 패키지 복원
    
    # 4. 빌드
    - name: Build
      run: dotnet build --no-restore
      # 프로젝트 빌드 (복원 생략)
    
    # 5. 테스트
    - name: Test
      run: dotnet test --no-build --verbosity normal
      # 테스트 실행 (빌드 생략)
```

---

## 22. 다중 .NET 버전 테스트

### Step 22.1: 매트릭스 빌드

```yaml
# .github/workflows/ci-matrix.yml
name: CI Matrix

on:
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]

jobs:
  build:
    runs-on: ubuntu-latest
    
    strategy:
      matrix:
        dotnet-version: [ '6.0.x', '7.0.x', '8.0.x' ]
    
    steps:
    - uses: actions/checkout@v4
    
    - name: Setup .NET ${{ matrix.dotnet-version }}
      uses: actions/setup-dotnet@v4
      with:
        dotnet-version: ${{ matrix.dotnet-version }}
    
    - name: Display .NET version
      run: dotnet --version
    
    - name: Restore
      run: dotnet restore
    
    - name: Build
      run: dotnet build --no-restore
    
    - name: Test
      run: dotnet test --no-build --verbosity normal
```

### Step 22.2: 매트릭스 결과

```
┌────────────────────────────────────────────────────────────────┐
│  매트릭스 빌드 결과                                            │
├────────────────────────────────────────────────────────────────┤
│                                                                │
│  CI Matrix                                                     │
│  ├── build (6.0.x)  ✅ 42s                                    │
│  ├── build (7.0.x)  ✅ 38s                                    │
│  └── build (8.0.x)  ✅ 35s                                    │
│                                                                │
│  3개의 작업이 병렬로 실행됨!                                   │
│                                                                │
└────────────────────────────────────────────────────────────────┘
```

---

## 23. 다중 OS 테스트

### Step 23.1: OS 매트릭스

```yaml
# .github/workflows/ci-os-matrix.yml
name: CI Multi-OS

on:
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]

jobs:
  build:
    strategy:
      matrix:
        os: [ ubuntu-latest, windows-latest, macos-latest ]
    
    runs-on: ${{ matrix.os }}
    
    steps:
    - uses: actions/checkout@v4
    
    - name: Setup .NET
      uses: actions/setup-dotnet@v4
      with:
        dotnet-version: '8.0.x'
    
    - name: Build and Test
      run: |
        dotnet restore
        dotnet build --no-restore
        dotnet test --no-build
```

---

## 24. 캐싱으로 속도 향상

### Step 24.1: NuGet 캐싱

```yaml
# .github/workflows/ci-cached.yml
name: CI with Cache

on:
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]

jobs:
  build:
    runs-on: ubuntu-latest
    
    steps:
    - uses: actions/checkout@v4
    
    - name: Setup .NET
      uses: actions/setup-dotnet@v4
      with:
        dotnet-version: '8.0.x'
    
    # NuGet 패키지 캐싱
    - name: Cache NuGet packages
      uses: actions/cache@v3
      with:
        path: ~/.nuget/packages
        key: ${{ runner.os }}-nuget-${{ hashFiles('**/*.csproj') }}
        restore-keys: |
          ${{ runner.os }}-nuget-
    
    - name: Restore
      run: dotnet restore
    
    - name: Build
      run: dotnet build --no-restore
    
    - name: Test
      run: dotnet test --no-build
```

### Step 24.2: 캐싱 효과

```
┌────────────────────────────────────────────────────────────────┐
│  캐싱 효과                                                     │
├────────────────────────────────────────────────────────────────┤
│                                                                │
│  캐싱 전:                                                      │
│  • Restore: 25초                                              │
│  • 전체: 65초                                                 │
│                                                                │
│  캐싱 후 (캐시 히트):                                          │
│  • Restore: 3초  ← 대폭 감소!                                 │
│  • 전체: 43초                                                 │
│                                                                │
│  캐시 키: runner.os + csproj 파일 해시                        │
│  → csproj 변경 시에만 새로 캐시                               │
│                                                                │
└────────────────────────────────────────────────────────────────┘
```

---

## 25. 테스트 결과 리포팅

### Step 25.1: 테스트 결과 업로드

```yaml
# .github/workflows/ci-test-report.yml
name: CI with Test Report

on:
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]

jobs:
  build:
    runs-on: ubuntu-latest
    
    steps:
    - uses: actions/checkout@v4
    
    - name: Setup .NET
      uses: actions/setup-dotnet@v4
      with:
        dotnet-version: '8.0.x'
    
    - name: Restore
      run: dotnet restore
    
    - name: Build
      run: dotnet build --no-restore
    
    - name: Test with TRX output
      run: dotnet test --no-build --logger "trx;LogFileName=test-results.trx"
    
    # 테스트 결과 업로드
    - name: Upload test results
      uses: actions/upload-artifact@v3
      if: always()  # 테스트 실패해도 업로드
      with:
        name: test-results
        path: '**/test-results.trx'
```

---

## 26. 빌드 아티팩트 저장

### Step 26.1: 아티팩트 업로드

```yaml
# .github/workflows/ci-artifact.yml
name: CI with Artifacts

on:
  push:
    branches: [ main ]

jobs:
  build:
    runs-on: ubuntu-latest
    
    steps:
    - uses: actions/checkout@v4
    
    - name: Setup .NET
      uses: actions/setup-dotnet@v4
      with:
        dotnet-version: '8.0.x'
    
    - name: Restore
      run: dotnet restore
    
    - name: Build
      run: dotnet build --configuration Release --no-restore
    
    - name: Test
      run: dotnet test --no-build --configuration Release
    
    # 빌드 결과물 발행
    - name: Publish
      run: dotnet publish src/MyCalculator/MyCalculator.csproj -c Release -o publish
    
    # 아티팩트 업로드
    - name: Upload artifact
      uses: actions/upload-artifact@v3
      with:
        name: MyCalculator
        path: publish/
        retention-days: 30  # 30일간 보관
```

---

## 27. 환경 변수 사용

### Step 27.1: 환경 변수 설정

```yaml
# .github/workflows/ci-env.yml
name: CI with Environment Variables

on:
  push:
    branches: [ main ]

# 워크플로우 수준 환경 변수
env:
  DOTNET_VERSION: '8.0.x'
  CONFIGURATION: 'Release'

jobs:
  build:
    runs-on: ubuntu-latest
    
    # 작업 수준 환경 변수
    env:
      BUILD_NUMBER: ${{ github.run_number }}
    
    steps:
    - uses: actions/checkout@v4
    
    - name: Setup .NET
      uses: actions/setup-dotnet@v4
      with:
        dotnet-version: ${{ env.DOTNET_VERSION }}
    
    - name: Display variables
      run: |
        echo "Configuration: ${{ env.CONFIGURATION }}"
        echo "Build Number: ${{ env.BUILD_NUMBER }}"
        echo "Branch: ${{ github.ref_name }}"
        echo "SHA: ${{ github.sha }}"
    
    - name: Build
      run: dotnet build --configuration ${{ env.CONFIGURATION }}
```

---

## 28. 시크릿 사용

### Step 28.1: GitHub 시크릿 설정

```
┌────────────────────────────────────────────────────────────────┐
│  GitHub 시크릿 설정                                            │
├────────────────────────────────────────────────────────────────┤
│                                                                │
│  시크릿 추가 방법:                                             │
│  1. 저장소 → Settings → Secrets and variables → Actions       │
│  2. "New repository secret" 클릭                              │
│  3. 이름과 값 입력                                            │
│                                                                │
│  예시 시크릿:                                                  │
│  • NUGET_API_KEY: NuGet 패키지 배포용                         │
│  • CODECOV_TOKEN: Codecov 업로드용                            │
│  • AZURE_CREDENTIALS: Azure 배포용                            │
│                                                                │
└────────────────────────────────────────────────────────────────┘
```

### Step 28.2: 워크플로우에서 시크릿 사용

```yaml
jobs:
  build:
    runs-on: ubuntu-latest
    
    steps:
    - uses: actions/checkout@v4
    
    # 시크릿 사용 예시
    - name: Use secret
      run: echo "Using secret..."
      env:
        API_KEY: ${{ secrets.NUGET_API_KEY }}
    
    # NuGet 배포 예시
    - name: Push to NuGet
      run: dotnet nuget push *.nupkg --api-key ${{ secrets.NUGET_API_KEY }}
```

---

## 29. 조건부 실행

### Step 29.1: if 조건문

```yaml
# .github/workflows/ci-conditional.yml
name: CI Conditional

on:
  push:
    branches: [ main, develop ]
  pull_request:
    branches: [ main ]

jobs:
  build:
    runs-on: ubuntu-latest
    
    steps:
    - uses: actions/checkout@v4
    
    - name: Setup .NET
      uses: actions/setup-dotnet@v4
      with:
        dotnet-version: '8.0.x'
    
    - name: Build
      run: dotnet build
    
    - name: Test
      run: dotnet test
    
    # main 브랜치에서만 실행
    - name: Deploy (main only)
      if: github.ref == 'refs/heads/main'
      run: echo "Deploying to production..."
    
    # develop 브랜치에서만 실행
    - name: Deploy to staging (develop only)
      if: github.ref == 'refs/heads/develop'
      run: echo "Deploying to staging..."
    
    # 이전 스텝 실패해도 실행
    - name: Cleanup (always)
      if: always()
      run: echo "Cleaning up..."
    
    # 이전 스텝 성공 시에만 실행
    - name: Notify success
      if: success()
      run: echo "Build succeeded!"
    
    # 이전 스텝 실패 시에만 실행
    - name: Notify failure
      if: failure()
      run: echo "Build failed!"
```

---

## 30. 작업 간 의존성

### Step 30.1: needs 키워드

```yaml
# .github/workflows/ci-jobs.yml
name: CI Multi Jobs

on:
  push:
    branches: [ main ]

jobs:
  # 첫 번째 작업: 빌드
  build:
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v4
    - name: Setup .NET
      uses: actions/setup-dotnet@v4
      with:
        dotnet-version: '8.0.x'
    - name: Build
      run: dotnet build
    - name: Upload build
      uses: actions/upload-artifact@v3
      with:
        name: build-output
        path: '**/bin/'
  
  # 두 번째 작업: 테스트 (빌드 후 실행)
  test:
    needs: build  # build 작업 완료 후 실행
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v4
    - name: Setup .NET
      uses: actions/setup-dotnet@v4
      with:
        dotnet-version: '8.0.x'
    - name: Download build
      uses: actions/download-artifact@v3
      with:
        name: build-output
    - name: Test
      run: dotnet test --no-build
  
  # 세 번째 작업: 배포 (빌드와 테스트 후 실행)
  deploy:
    needs: [ build, test ]  # 둘 다 완료 후 실행
    runs-on: ubuntu-latest
    if: github.ref == 'refs/heads/main'
    steps:
    - name: Deploy
      run: echo "Deploying..."
```

### Step 30.2: 작업 흐름 시각화

```
┌────────────────────────────────────────────────────────────────┐
│  작업 흐름                                                     │
├────────────────────────────────────────────────────────────────┤
│                                                                │
│  ┌─────────────────┐                                           │
│  │     build       │                                           │
│  └────────┬────────┘                                           │
│           │                                                    │
│           ▼                                                    │
│  ┌─────────────────┐                                           │
│  │     test        │                                           │
│  └────────┬────────┘                                           │
│           │                                                    │
│           ▼                                                    │
│  ┌─────────────────┐                                           │
│  │    deploy       │  (main 브랜치만)                          │
│  └─────────────────┘                                           │
│                                                                │
└────────────────────────────────────────────────────────────────┘
```

---

## 31. 타임아웃 설정

### Step 31.1: 작업 및 스텝 타임아웃

```yaml
jobs:
  build:
    runs-on: ubuntu-latest
    timeout-minutes: 30  # 작업 전체 타임아웃
    
    steps:
    - uses: actions/checkout@v4
    
    - name: Long running task
      timeout-minutes: 10  # 개별 스텝 타임아웃
      run: |
        # 오래 걸리는 작업
        dotnet test
```

---

## 32. 완성된 CI 워크플로우

### Step 32.1: 종합 CI 워크플로우

```yaml
# .github/workflows/ci.yml
name: CI

on:
  push:
    branches: [ main, develop ]
  pull_request:
    branches: [ main ]

env:
  DOTNET_VERSION: '8.0.x'

jobs:
  build-and-test:
    runs-on: ubuntu-latest
    timeout-minutes: 15
    
    steps:
    - name: Checkout
      uses: actions/checkout@v4
    
    - name: Setup .NET
      uses: actions/setup-dotnet@v4
      with:
        dotnet-version: ${{ env.DOTNET_VERSION }}
    
    - name: Cache NuGet
      uses: actions/cache@v3
      with:
        path: ~/.nuget/packages
        key: ${{ runner.os }}-nuget-${{ hashFiles('**/*.csproj') }}
        restore-keys: |
          ${{ runner.os }}-nuget-
    
    - name: Restore
      run: dotnet restore
    
    - name: Build
      run: dotnet build --no-restore --configuration Release
    
    - name: Test
      run: dotnet test --no-build --configuration Release --verbosity normal
    
    - name: Upload test results
      uses: actions/upload-artifact@v3
      if: always()
      with:
        name: test-results
        path: '**/TestResults/*.trx'
        retention-days: 7
```

---

이어서 Part 2를 작성하겠습니다.
# 🚀 C# CI/CD 통합 따라하기 실습 - Part 2

---

# Part 4: PR 자동 테스트

---

## 33. PR 워크플로우 개요

### Step 33.1: PR 자동 테스트의 목적

```
┌────────────────────────────────────────────────────────────────┐
│  PR 자동 테스트의 목적                                         │
├────────────────────────────────────────────────────────────────┤
│                                                                │
│  1. 코드 품질 게이트                                           │
│     • 테스트 실패 시 머지 방지                                │
│     • 빌드 실패 시 머지 방지                                  │
│                                                                │
│  2. 빠른 피드백                                                │
│     • PR 생성 즉시 테스트 실행                                │
│     • 결과를 PR 페이지에서 확인                               │
│                                                                │
│  3. 코드 리뷰 효율화                                           │
│     • 기계적 검사 자동화                                      │
│     • 리뷰어는 로직에 집중                                    │
│                                                                │
│  ┌──────────────────────────────────────────────────────────┐ │
│  │  PR 생성 → 자동 테스트 → 결과 표시 → 머지 가능/불가      │ │
│  └──────────────────────────────────────────────────────────┘ │
│                                                                │
└────────────────────────────────────────────────────────────────┘
```

---

## 34. PR 전용 워크플로우

### Step 34.1: pr-check.yml

```yaml
# .github/workflows/pr-check.yml
name: PR Check

on:
  pull_request:
    branches: [ main, develop ]
    types: [ opened, synchronize, reopened ]

jobs:
  check:
    runs-on: ubuntu-latest
    
    steps:
    - name: Checkout
      uses: actions/checkout@v4
    
    - name: Setup .NET
      uses: actions/setup-dotnet@v4
      with:
        dotnet-version: '8.0.x'
    
    - name: Restore
      run: dotnet restore
    
    - name: Build
      run: dotnet build --no-restore
    
    - name: Test
      run: dotnet test --no-build --verbosity normal
```

### Step 34.2: PR 이벤트 타입

```
┌────────────────────────────────────────────────────────────────┐
│  PR 이벤트 타입                                                │
├────────────────────────────────────────────────────────────────┤
│                                                                │
│  types: [ opened, synchronize, reopened ]                      │
│                                                                │
│  • opened: PR이 처음 생성될 때                                │
│  • synchronize: PR에 새 커밋이 푸시될 때                      │
│  • reopened: 닫힌 PR이 다시 열릴 때                           │
│                                                                │
│  기본값: opened, synchronize, reopened                         │
│  (명시하지 않아도 이 3가지가 기본)                             │
│                                                                │
│  기타 타입:                                                    │
│  • closed: PR이 닫힐 때                                       │
│  • ready_for_review: Draft에서 Ready로 변경                   │
│  • labeled: 라벨이 추가될 때                                  │
│  • review_requested: 리뷰 요청 시                             │
│                                                                │
└────────────────────────────────────────────────────────────────┘
```

---

## 35. PR 상태 체크

### Step 35.1: GitHub PR 상태 표시

```
┌────────────────────────────────────────────────────────────────┐
│  PR 페이지에서 상태 체크 확인                                  │
├────────────────────────────────────────────────────────────────┤
│                                                                │
│  ┌──────────────────────────────────────────────────────────┐ │
│  │  Pull Request #42: Add new feature                       │ │
│  ├──────────────────────────────────────────────────────────┤ │
│  │                                                          │ │
│  │  Checks                                                  │ │
│  │  ─────────────────────────────────────────────────────── │ │
│  │  ✅ PR Check / check (pull_request)     Successful       │ │
│  │  ✅ CI / build-and-test (push)          Successful       │ │
│  │                                                          │ │
│  │  ─────────────────────────────────────────────────────── │ │
│  │  ✅ All checks have passed                              │ │
│  │                                                          │ │
│  │  [Merge pull request]                                    │ │
│  │                                                          │ │
│  └──────────────────────────────────────────────────────────┘ │
│                                                                │
│  테스트 실패 시:                                               │
│  ┌──────────────────────────────────────────────────────────┐ │
│  │  ❌ PR Check / check (pull_request)     Failing          │ │
│  │                                                          │ │
│  │  ─────────────────────────────────────────────────────── │ │
│  │  ❌ Some checks were not successful                      │ │
│  │                                                          │ │
│  │  [Merge pull request] (비활성화 또는 경고)               │ │
│  └──────────────────────────────────────────────────────────┘ │
│                                                                │
└────────────────────────────────────────────────────────────────┘
```

---

## 36. 브랜치 보호 규칙

### Step 36.1: 브랜치 보호 설정

```
┌────────────────────────────────────────────────────────────────┐
│  브랜치 보호 규칙 설정                                         │
├────────────────────────────────────────────────────────────────┤
│                                                                │
│  설정 경로:                                                    │
│  Settings → Branches → Add branch protection rule              │
│                                                                │
│  권장 설정:                                                    │
│  ┌──────────────────────────────────────────────────────────┐ │
│  │                                                          │ │
│  │  Branch name pattern: main                               │ │
│  │                                                          │ │
│  │  ☑ Require a pull request before merging                │ │
│  │    ☑ Require approvals: 1                               │ │
│  │    ☑ Dismiss stale PR approvals when new commits pushed │ │
│  │                                                          │ │
│  │  ☑ Require status checks to pass before merging         │ │
│  │    ☑ Require branches to be up to date before merging   │ │
│  │    Status checks:                                        │ │
│  │      ☑ PR Check / check                                 │ │
│  │      ☑ CI / build-and-test                              │ │
│  │                                                          │ │
│  │  ☑ Require conversation resolution before merging       │ │
│  │                                                          │ │
│  │  ☐ Require signed commits                               │ │
│  │                                                          │ │
│  │  ☑ Do not allow bypassing the above settings            │ │
│  │                                                          │ │
│  └──────────────────────────────────────────────────────────┘ │
│                                                                │
└────────────────────────────────────────────────────────────────┘
```

---

## 37. Required Status Checks 설정

### Step 37.1: 필수 상태 체크

```
┌────────────────────────────────────────────────────────────────┐
│  필수 상태 체크 설정                                           │
├────────────────────────────────────────────────────────────────┤
│                                                                │
│  1. 브랜치 보호 규칙에서 "Require status checks" 활성화       │
│                                                                │
│  2. 검색창에서 워크플로우 이름 검색                           │
│     • "PR Check"                                              │
│     • 자동 완성에서 선택                                      │
│                                                                │
│  3. 저장                                                       │
│                                                                │
│  결과:                                                         │
│  • PR Check가 통과해야만 머지 가능                            │
│  • 테스트 실패 시 머지 버튼 비활성화                          │
│                                                                │
│  주의:                                                         │
│  • 워크플로우가 최소 1번 실행된 후 선택 가능                  │
│  • 작업 이름 형식: "workflow-name / job-name"                 │
│                                                                │
└────────────────────────────────────────────────────────────────┘
```

---

## 38. PR 코멘트에 결과 표시

### Step 38.1: 테스트 결과 코멘트

```yaml
# .github/workflows/pr-check.yml (개선)
name: PR Check

on:
  pull_request:
    branches: [ main ]

jobs:
  check:
    runs-on: ubuntu-latest
    
    steps:
    - uses: actions/checkout@v4
    
    - name: Setup .NET
      uses: actions/setup-dotnet@v4
      with:
        dotnet-version: '8.0.x'
    
    - name: Build and Test
      run: |
        dotnet restore
        dotnet build --no-restore
        dotnet test --no-build --logger "trx;LogFileName=test-results.trx"
    
    # 테스트 결과를 PR 코멘트로 표시
    - name: Test Report
      uses: dorny/test-reporter@v1
      if: success() || failure()
      with:
        name: Test Results
        path: '**/test-results.trx'
        reporter: dotnet-trx
```

### Step 38.2: 코멘트 예시

```
┌────────────────────────────────────────────────────────────────┐
│  PR 코멘트에 테스트 결과 표시                                  │
├────────────────────────────────────────────────────────────────┤
│                                                                │
│  ┌──────────────────────────────────────────────────────────┐ │
│  │  github-actions bot                                      │ │
│  │                                                          │ │
│  │  📋 Test Results                                         │ │
│  │                                                          │ │
│  │  ✅ 12 tests passed                                      │ │
│  │  ❌ 0 tests failed                                       │ │
│  │  ⏭️ 0 tests skipped                                      │ │
│  │                                                          │ │
│  │  Duration: 2.34s                                         │ │
│  │                                                          │ │
│  │  Details:                                                │ │
│  │  ├── MyCalculator.Tests                                  │ │
│  │  │   ├── ✅ Add_TwoNumbers_ReturnsSum (0.01s)           │ │
│  │  │   ├── ✅ Subtract_TwoNumbers_ReturnsDifference       │ │
│  │  │   ├── ✅ Multiply_TwoNumbers_ReturnsProduct          │ │
│  │  │   └── ...                                             │ │
│  │                                                          │ │
│  └──────────────────────────────────────────────────────────┘ │
│                                                                │
└────────────────────────────────────────────────────────────────┘
```

---

## 39. PR 라벨 자동화

### Step 39.1: 라벨러 설정

```yaml
# .github/labeler.yml
# 파일 경로에 따라 자동 라벨 부여

documentation:
  - '**/*.md'
  - 'docs/**'

tests:
  - 'tests/**'
  - '**/*Tests.cs'

source:
  - 'src/**'

ci:
  - '.github/**'
```

```yaml
# .github/workflows/labeler.yml
name: Labeler

on:
  pull_request:
    types: [ opened, synchronize ]

jobs:
  label:
    runs-on: ubuntu-latest
    permissions:
      contents: read
      pull-requests: write
    
    steps:
    - uses: actions/labeler@v4
      with:
        repo-token: ${{ secrets.GITHUB_TOKEN }}
```

---

## 40. PR 크기 체크

### Step 40.1: PR 크기 경고

```yaml
# .github/workflows/pr-size.yml
name: PR Size Check

on:
  pull_request:
    types: [ opened, synchronize ]

jobs:
  size-check:
    runs-on: ubuntu-latest
    
    steps:
    - uses: actions/checkout@v4
      with:
        fetch-depth: 0
    
    - name: Check PR size
      run: |
        # 변경된 줄 수 계산
        ADDITIONS=$(git diff origin/${{ github.base_ref }}...HEAD --numstat | awk '{sum+=$1} END {print sum}')
        DELETIONS=$(git diff origin/${{ github.base_ref }}...HEAD --numstat | awk '{sum+=$2} END {print sum}')
        TOTAL=$((ADDITIONS + DELETIONS))
        
        echo "Additions: $ADDITIONS"
        echo "Deletions: $DELETIONS"
        echo "Total: $TOTAL"
        
        # 500줄 초과 시 경고
        if [ $TOTAL -gt 500 ]; then
          echo "::warning::PR size is large ($TOTAL lines). Consider splitting into smaller PRs."
        fi
        
        # 1000줄 초과 시 실패
        if [ $TOTAL -gt 1000 ]; then
          echo "::error::PR is too large ($TOTAL lines). Please split into smaller PRs."
          exit 1
        fi
```

---

## 41. Draft PR 처리

### Step 41.1: Draft PR 스킵

```yaml
# .github/workflows/pr-check.yml
name: PR Check

on:
  pull_request:
    branches: [ main ]
    types: [ opened, synchronize, reopened, ready_for_review ]

jobs:
  check:
    # Draft PR이 아닐 때만 실행
    if: github.event.pull_request.draft == false
    runs-on: ubuntu-latest
    
    steps:
    - uses: actions/checkout@v4
    
    - name: Setup .NET
      uses: actions/setup-dotnet@v4
      with:
        dotnet-version: '8.0.x'
    
    - name: Build and Test
      run: |
        dotnet restore
        dotnet build --no-restore
        dotnet test --no-build
```

---

## 42. 완성된 PR 워크플로우

### Step 42.1: 종합 PR 워크플로우

```yaml
# .github/workflows/pr-check.yml
name: PR Check

on:
  pull_request:
    branches: [ main, develop ]
    types: [ opened, synchronize, reopened, ready_for_review ]

concurrency:
  group: ${{ github.workflow }}-${{ github.event.pull_request.number || github.ref }}
  cancel-in-progress: true

jobs:
  check:
    if: github.event.pull_request.draft == false
    runs-on: ubuntu-latest
    timeout-minutes: 10
    
    steps:
    - name: Checkout
      uses: actions/checkout@v4
    
    - name: Setup .NET
      uses: actions/setup-dotnet@v4
      with:
        dotnet-version: '8.0.x'
    
    - name: Cache NuGet
      uses: actions/cache@v3
      with:
        path: ~/.nuget/packages
        key: ${{ runner.os }}-nuget-${{ hashFiles('**/*.csproj') }}
        restore-keys: |
          ${{ runner.os }}-nuget-
    
    - name: Restore
      run: dotnet restore
    
    - name: Build
      run: dotnet build --no-restore
    
    - name: Test
      run: dotnet test --no-build --logger "trx;LogFileName=test-results.trx"
    
    - name: Test Report
      uses: dorny/test-reporter@v1
      if: success() || failure()
      with:
        name: Test Results
        path: '**/test-results.trx'
        reporter: dotnet-trx
```

---

# Part 5: 코드 품질 체크

---

## 43. 커버리지 측정

### Step 43.1: Coverlet 통합

```yaml
# .github/workflows/coverage.yml
name: Coverage

on:
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]

jobs:
  coverage:
    runs-on: ubuntu-latest
    
    steps:
    - uses: actions/checkout@v4
    
    - name: Setup .NET
      uses: actions/setup-dotnet@v4
      with:
        dotnet-version: '8.0.x'
    
    - name: Restore
      run: dotnet restore
    
    - name: Build
      run: dotnet build --no-restore
    
    - name: Test with coverage
      run: |
        dotnet test --no-build \
          --collect:"XPlat Code Coverage" \
          --results-directory ./coverage
    
    - name: Upload coverage
      uses: actions/upload-artifact@v3
      with:
        name: coverage-report
        path: coverage/**/coverage.cobertura.xml
```

---

## 44. Codecov 연동

### Step 44.1: Codecov 업로드

```yaml
# .github/workflows/coverage.yml (Codecov 추가)
name: Coverage

on:
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]

jobs:
  coverage:
    runs-on: ubuntu-latest
    
    steps:
    - uses: actions/checkout@v4
    
    - name: Setup .NET
      uses: actions/setup-dotnet@v4
      with:
        dotnet-version: '8.0.x'
    
    - name: Test with coverage
      run: |
        dotnet restore
        dotnet build --no-restore
        dotnet test --no-build --collect:"XPlat Code Coverage"
    
    # Codecov에 업로드
    - name: Upload to Codecov
      uses: codecov/codecov-action@v3
      with:
        files: ./**/coverage.cobertura.xml
        fail_ci_if_error: true
        verbose: true
```

### Step 44.2: Codecov 설정 파일

```yaml
# codecov.yml (저장소 루트)
coverage:
  status:
    project:
      default:
        target: 80%        # 목표 커버리지
        threshold: 5%      # 허용 오차
    patch:
      default:
        target: 80%        # 새 코드 커버리지

comment:
  layout: "reach,diff,flags,files"
  behavior: default
  require_changes: true
```

---

## 45. 커버리지 뱃지

### Step 45.1: README에 뱃지 추가

```markdown
# MyCalculator

[![CI](https://github.com/username/MyCalculator/actions/workflows/ci.yml/badge.svg)](https://github.com/username/MyCalculator/actions/workflows/ci.yml)
[![codecov](https://codecov.io/gh/username/MyCalculator/branch/main/graph/badge.svg)](https://codecov.io/gh/username/MyCalculator)

## Description
A simple calculator library with CI/CD integration.
```

---

## 46. 커버리지 임계값 강제

### Step 46.1: 빌드 실패 설정

```yaml
# .github/workflows/coverage.yml
- name: Test with coverage threshold
  run: |
    dotnet test --no-build \
      /p:CollectCoverage=true \
      /p:CoverletOutputFormat=cobertura \
      /p:Threshold=80 \
      /p:ThresholdType=line \
      /p:ThresholdStat=total
```

---

## 47. 코드 포맷팅 체크

### Step 47.1: dotnet format 사용

```yaml
# .github/workflows/format-check.yml
name: Format Check

on:
  pull_request:
    branches: [ main ]

jobs:
  format:
    runs-on: ubuntu-latest
    
    steps:
    - uses: actions/checkout@v4
    
    - name: Setup .NET
      uses: actions/setup-dotnet@v4
      with:
        dotnet-version: '8.0.x'
    
    - name: Check formatting
      run: |
        dotnet format --verify-no-changes --verbosity diagnostic
```

### Step 47.2: .editorconfig 설정

```ini
# .editorconfig
root = true

[*]
indent_style = space
indent_size = 4
end_of_line = lf
charset = utf-8
trim_trailing_whitespace = true
insert_final_newline = true

[*.cs]
# 네이밍 규칙
dotnet_naming_rule.private_fields_should_be_camel_case.severity = warning
dotnet_naming_rule.private_fields_should_be_camel_case.symbols = private_fields
dotnet_naming_rule.private_fields_should_be_camel_case.style = camel_case_style

dotnet_naming_symbols.private_fields.applicable_kinds = field
dotnet_naming_symbols.private_fields.applicable_accessibilities = private

dotnet_naming_style.camel_case_style.capitalization = camel_case
dotnet_naming_style.camel_case_style.required_prefix = _

# 코드 스타일
csharp_style_var_for_built_in_types = true:suggestion
csharp_style_var_when_type_is_apparent = true:suggestion
```

---

## 48. 정적 분석 (Roslyn Analyzers)

### Step 48.1: 분석기 패키지 추가

```xml
<!-- src/MyCalculator/MyCalculator.csproj -->
<Project Sdk="Microsoft.NET.Sdk">
  <PropertyGroup>
    <TargetFramework>net8.0</TargetFramework>
    <TreatWarningsAsErrors>true</TreatWarningsAsErrors>
    <AnalysisMode>AllEnabledByDefault</AnalysisMode>
  </PropertyGroup>
  
  <ItemGroup>
    <PackageReference Include="Microsoft.CodeAnalysis.NetAnalyzers" Version="8.0.0">
      <PrivateAssets>all</PrivateAssets>
      <IncludeAssets>runtime; build; native; contentfiles; analyzers</IncludeAssets>
    </PackageReference>
  </ItemGroup>
</Project>
```

---

## 49. 보안 취약점 스캔

### Step 49.1: dotnet list package --vulnerable

```yaml
# .github/workflows/security.yml
name: Security Scan

on:
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]
  schedule:
    - cron: '0 0 * * 1'  # 매주 월요일

jobs:
  security:
    runs-on: ubuntu-latest
    
    steps:
    - uses: actions/checkout@v4
    
    - name: Setup .NET
      uses: actions/setup-dotnet@v4
      with:
        dotnet-version: '8.0.x'
    
    - name: Restore
      run: dotnet restore
    
    - name: Check for vulnerable packages
      run: |
        dotnet list package --vulnerable --include-transitive 2>&1 | tee vulnerability-report.txt
        
        # 취약점이 있으면 실패
        if grep -q "has the following vulnerable packages" vulnerability-report.txt; then
          echo "::error::Vulnerable packages found!"
          exit 1
        fi
    
    - name: Upload vulnerability report
      uses: actions/upload-artifact@v3
      if: always()
      with:
        name: vulnerability-report
        path: vulnerability-report.txt
```

---

## 50. Dependabot 설정

### Step 50.1: dependabot.yml

```yaml
# .github/dependabot.yml
version: 2

updates:
  # NuGet 패키지
  - package-ecosystem: "nuget"
    directory: "/"
    schedule:
      interval: "weekly"
      day: "monday"
    open-pull-requests-limit: 5
    labels:
      - "dependencies"
      - "nuget"
    commit-message:
      prefix: "nuget"
    
  # GitHub Actions
  - package-ecosystem: "github-actions"
    directory: "/"
    schedule:
      interval: "weekly"
    labels:
      - "dependencies"
      - "github-actions"
    commit-message:
      prefix: "ci"
```

---

## 51. 종합 품질 체크 워크플로우

### Step 51.1: quality.yml

```yaml
# .github/workflows/quality.yml
name: Code Quality

on:
  pull_request:
    branches: [ main ]

jobs:
  quality:
    runs-on: ubuntu-latest
    
    steps:
    - uses: actions/checkout@v4
    
    - name: Setup .NET
      uses: actions/setup-dotnet@v4
      with:
        dotnet-version: '8.0.x'
    
    - name: Restore
      run: dotnet restore
    
    # 포맷팅 체크
    - name: Check formatting
      run: dotnet format --verify-no-changes
    
    # 빌드 (경고를 에러로)
    - name: Build with warnings as errors
      run: dotnet build --no-restore -warnaserror
    
    # 테스트 + 커버리지
    - name: Test with coverage
      run: |
        dotnet test --no-build \
          /p:CollectCoverage=true \
          /p:CoverletOutputFormat=cobertura \
          /p:Threshold=80
    
    # 보안 스캔
    - name: Security scan
      run: |
        dotnet list package --vulnerable --include-transitive
```

---

## 52. 품질 게이트 요약

### Step 52.1: 품질 게이트 체크리스트

```
┌────────────────────────────────────────────────────────────────┐
│  품질 게이트 체크리스트                                        │
├────────────────────────────────────────────────────────────────┤
│                                                                │
│  빌드 체크                                                     │
│  ☑ 컴파일 성공                                                │
│  ☑ 경고 0개 (TreatWarningsAsErrors)                           │
│                                                                │
│  테스트 체크                                                   │
│  ☑ 모든 테스트 통과                                           │
│  ☑ 커버리지 80% 이상                                          │
│                                                                │
│  코드 품질                                                     │
│  ☑ 포맷팅 규칙 준수                                           │
│  ☑ 정적 분석 통과                                             │
│  ☑ 보안 취약점 없음                                           │
│                                                                │
│  PR 규칙                                                       │
│  ☑ 리뷰어 승인                                                │
│  ☑ 모든 상태 체크 통과                                        │
│  ☑ 최신 브랜치와 동기화                                       │
│                                                                │
└────────────────────────────────────────────────────────────────┘
```

---

# Part 6: 고급 워크플로우

---

## 53. 재사용 가능한 워크플로우

### Step 53.1: 재사용 가능 워크플로우 정의

```yaml
# .github/workflows/reusable-build.yml
name: Reusable Build

on:
  workflow_call:
    inputs:
      dotnet-version:
        description: '.NET version'
        required: false
        type: string
        default: '8.0.x'
      configuration:
        description: 'Build configuration'
        required: false
        type: string
        default: 'Release'
    secrets:
      NUGET_API_KEY:
        required: false

jobs:
  build:
    runs-on: ubuntu-latest
    
    steps:
    - uses: actions/checkout@v4
    
    - name: Setup .NET
      uses: actions/setup-dotnet@v4
      with:
        dotnet-version: ${{ inputs.dotnet-version }}
    
    - name: Build
      run: |
        dotnet restore
        dotnet build --configuration ${{ inputs.configuration }}
        dotnet test --configuration ${{ inputs.configuration }}
```

### Step 53.2: 재사용 워크플로우 호출

```yaml
# .github/workflows/ci.yml
name: CI

on:
  push:
    branches: [ main ]

jobs:
  build:
    uses: ./.github/workflows/reusable-build.yml
    with:
      dotnet-version: '8.0.x'
      configuration: 'Release'
    secrets:
      NUGET_API_KEY: ${{ secrets.NUGET_API_KEY }}
```

---

## 54. 컴포짓 액션

### Step 54.1: 커스텀 액션 정의

```yaml
# .github/actions/dotnet-setup/action.yml
name: 'Setup .NET and Restore'
description: 'Setup .NET SDK and restore NuGet packages'

inputs:
  dotnet-version:
    description: '.NET version'
    required: false
    default: '8.0.x'

runs:
  using: 'composite'
  steps:
    - name: Setup .NET
      uses: actions/setup-dotnet@v4
      with:
        dotnet-version: ${{ inputs.dotnet-version }}
    
    - name: Cache NuGet
      uses: actions/cache@v3
      with:
        path: ~/.nuget/packages
        key: ${{ runner.os }}-nuget-${{ hashFiles('**/*.csproj') }}
        restore-keys: |
          ${{ runner.os }}-nuget-
    
    - name: Restore
      shell: bash
      run: dotnet restore
```

### Step 54.2: 컴포짓 액션 사용

```yaml
# .github/workflows/ci.yml
jobs:
  build:
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v4
    
    - name: Setup .NET and Restore
      uses: ./.github/actions/dotnet-setup
      with:
        dotnet-version: '8.0.x'
    
    - name: Build
      run: dotnet build --no-restore
```

---

## 55. 매트릭스 전략 고급

### Step 55.1: 복합 매트릭스

```yaml
jobs:
  build:
    strategy:
      fail-fast: false  # 하나 실패해도 나머지 계속 실행
      matrix:
        os: [ ubuntu-latest, windows-latest, macos-latest ]
        dotnet: [ '6.0.x', '7.0.x', '8.0.x' ]
        exclude:
          # macOS에서 .NET 6는 제외
          - os: macos-latest
            dotnet: '6.0.x'
        include:
          # 특정 조합에 추가 변수
          - os: ubuntu-latest
            dotnet: '8.0.x'
            coverage: true
    
    runs-on: ${{ matrix.os }}
    
    steps:
    - uses: actions/checkout@v4
    
    - name: Setup .NET ${{ matrix.dotnet }}
      uses: actions/setup-dotnet@v4
      with:
        dotnet-version: ${{ matrix.dotnet }}
    
    - name: Build and Test
      run: |
        dotnet build
        dotnet test
    
    - name: Coverage
      if: matrix.coverage == true
      run: dotnet test --collect:"XPlat Code Coverage"
```

---

## 56. 워크플로우 동시성 제어

### Step 56.1: concurrency 설정

```yaml
# 같은 브랜치/PR에서 실행 중인 워크플로우 취소
name: CI

on:
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]

concurrency:
  # 그룹: 워크플로우 + PR 번호 또는 브랜치
  group: ${{ github.workflow }}-${{ github.event.pull_request.number || github.ref }}
  # 진행 중인 실행 취소
  cancel-in-progress: true

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v4
    # ...
```

---

이어서 Part 3에서 계속됩니다.
# 🚀 C# CI/CD 통합 따라하기 실습 - Part 3

---

## 57. 수동 워크플로우 실행 (계속)

### Step 57.1: workflow_dispatch 상세

```yaml
# .github/workflows/manual-deploy.yml
name: Manual Deploy

on:
  workflow_dispatch:
    inputs:
      environment:
        description: 'Deployment environment'
        required: true
        type: choice
        options:
          - staging
          - production
        default: staging
      version:
        description: 'Version to deploy'
        required: true
        type: string
      dry-run:
        description: 'Dry run (no actual deployment)'
        required: false
        type: boolean
        default: false

jobs:
  deploy:
    runs-on: ubuntu-latest
    
    steps:
    - uses: actions/checkout@v4
    
    - name: Display inputs
      run: |
        echo "Environment: ${{ inputs.environment }}"
        echo "Version: ${{ inputs.version }}"
        echo "Dry run: ${{ inputs.dry-run }}"
    
    - name: Deploy
      if: inputs.dry-run == false
      run: |
        echo "Deploying version ${{ inputs.version }} to ${{ inputs.environment }}..."
        # 실제 배포 명령어
```

### Step 57.2: 수동 실행 방법

```
┌────────────────────────────────────────────────────────────────┐
│  수동 워크플로우 실행                                          │
├────────────────────────────────────────────────────────────────┤
│                                                                │
│  1. GitHub 저장소 → Actions 탭                                │
│  2. 왼쪽에서 "Manual Deploy" 워크플로우 선택                  │
│  3. "Run workflow" 버튼 클릭                                  │
│  4. 입력값 설정                                               │
│     • Environment: staging / production                       │
│     • Version: 1.0.0                                          │
│     • Dry run: ☑ / ☐                                         │
│  5. "Run workflow" 클릭                                       │
│                                                                │
└────────────────────────────────────────────────────────────────┘
```

---

## 58. 스케줄 워크플로우

### Step 58.1: cron 스케줄

```yaml
# .github/workflows/scheduled.yml
name: Scheduled Tasks

on:
  schedule:
    # 매일 자정 (UTC)
    - cron: '0 0 * * *'
    # 매주 월요일 오전 9시 (UTC)
    - cron: '0 9 * * 1'
  
  # 수동 실행도 가능하게
  workflow_dispatch:

jobs:
  nightly-build:
    runs-on: ubuntu-latest
    
    steps:
    - uses: actions/checkout@v4
    
    - name: Setup .NET
      uses: actions/setup-dotnet@v4
      with:
        dotnet-version: '8.0.x'
    
    - name: Build and Test
      run: |
        dotnet restore
        dotnet build
        dotnet test
    
    - name: Security scan
      run: dotnet list package --vulnerable --include-transitive
    
    - name: Notify on failure
      if: failure()
      run: echo "Nightly build failed!"
```

### Step 58.2: Cron 문법

```
┌────────────────────────────────────────────────────────────────┐
│  Cron 문법                                                     │
├────────────────────────────────────────────────────────────────┤
│                                                                │
│  ┌───────────── 분 (0 - 59)                                   │
│  │ ┌─────────── 시 (0 - 23)                                   │
│  │ │ ┌───────── 일 (1 - 31)                                   │
│  │ │ │ ┌─────── 월 (1 - 12)                                   │
│  │ │ │ │ ┌───── 요일 (0 - 6, 일요일 = 0)                      │
│  │ │ │ │ │                                                    │
│  * * * * *                                                     │
│                                                                │
│  예시:                                                         │
│  '0 0 * * *'     매일 자정                                    │
│  '0 9 * * 1'     매주 월요일 09:00                            │
│  '0 */6 * * *'   6시간마다                                    │
│  '0 0 1 * *'     매월 1일 자정                                │
│  '0 0 * * 0'     매주 일요일 자정                             │
│                                                                │
│  주의: GitHub Actions의 시간은 UTC 기준!                       │
│  한국시간(KST) = UTC + 9시간                                  │
│                                                                │
└────────────────────────────────────────────────────────────────┘
```

---

## 59. 릴리스 워크플로우

### Step 59.1: 태그 기반 릴리스

```yaml
# .github/workflows/release.yml
name: Release

on:
  push:
    tags:
      - 'v*'  # v로 시작하는 태그 (v1.0.0, v2.1.0 등)

jobs:
  release:
    runs-on: ubuntu-latest
    
    steps:
    - uses: actions/checkout@v4
    
    - name: Setup .NET
      uses: actions/setup-dotnet@v4
      with:
        dotnet-version: '8.0.x'
    
    - name: Get version from tag
      id: version
      run: echo "VERSION=${GITHUB_REF#refs/tags/v}" >> $GITHUB_OUTPUT
    
    - name: Build
      run: |
        dotnet restore
        dotnet build --configuration Release /p:Version=${{ steps.version.outputs.VERSION }}
    
    - name: Test
      run: dotnet test --configuration Release --no-build
    
    - name: Pack
      run: dotnet pack --configuration Release --no-build -o ./nupkg /p:Version=${{ steps.version.outputs.VERSION }}
    
    - name: Create GitHub Release
      uses: softprops/action-gh-release@v1
      with:
        files: |
          ./nupkg/*.nupkg
        generate_release_notes: true
      env:
        GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
    
    # NuGet 배포 (선택)
    - name: Push to NuGet
      if: github.event_name == 'push' && startsWith(github.ref, 'refs/tags/')
      run: |
        dotnet nuget push ./nupkg/*.nupkg \
          --api-key ${{ secrets.NUGET_API_KEY }} \
          --source https://api.nuget.org/v3/index.json
```

---

## 60. 환경(Environment) 사용

### Step 60.1: 환경 설정

```yaml
# .github/workflows/deploy.yml
name: Deploy

on:
  push:
    branches: [ main ]

jobs:
  deploy-staging:
    runs-on: ubuntu-latest
    environment: staging  # 환경 지정
    
    steps:
    - uses: actions/checkout@v4
    
    - name: Deploy to Staging
      run: echo "Deploying to staging..."
      env:
        API_URL: ${{ vars.API_URL }}  # 환경 변수
        API_KEY: ${{ secrets.API_KEY }}  # 환경 시크릿
  
  deploy-production:
    needs: deploy-staging
    runs-on: ubuntu-latest
    environment: 
      name: production
      url: https://myapp.com  # 배포 URL
    
    steps:
    - uses: actions/checkout@v4
    
    - name: Deploy to Production
      run: echo "Deploying to production..."
```

### Step 60.2: 환경 보호 규칙

```
┌────────────────────────────────────────────────────────────────┐
│  환경 보호 규칙 설정                                           │
├────────────────────────────────────────────────────────────────┤
│                                                                │
│  Settings → Environments → New environment                     │
│                                                                │
│  Production 환경 예시:                                         │
│  ┌──────────────────────────────────────────────────────────┐ │
│  │  Environment: production                                 │ │
│  │                                                          │ │
│  │  Protection rules:                                       │ │
│  │  ☑ Required reviewers                                   │ │
│  │    • @team-leads                                        │ │
│  │                                                          │ │
│  │  ☑ Wait timer                                           │ │
│  │    • 30 minutes                                         │ │
│  │                                                          │ │
│  │  ☑ Restrict deployments to selected branches            │ │
│  │    • main                                               │ │
│  │                                                          │ │
│  │  Environment secrets:                                    │ │
│  │    • API_KEY                                            │ │
│  │    • DATABASE_URL                                       │ │
│  │                                                          │ │
│  │  Environment variables:                                  │ │
│  │    • API_URL = https://api.myapp.com                    │ │
│  │                                                          │ │
│  └──────────────────────────────────────────────────────────┘ │
│                                                                │
└────────────────────────────────────────────────────────────────┘
```

---

## 61. 알림 설정

### Step 61.1: Slack 알림

```yaml
# .github/workflows/ci.yml
jobs:
  build:
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v4
    
    - name: Build and Test
      run: |
        dotnet build
        dotnet test
    
    # 실패 시 Slack 알림
    - name: Slack Notification on Failure
      if: failure()
      uses: slackapi/slack-github-action@v1
      with:
        payload: |
          {
            "text": "❌ Build Failed!",
            "blocks": [
              {
                "type": "section",
                "text": {
                  "type": "mrkdwn",
                  "text": "*Build Failed* :x:\n*Repository:* ${{ github.repository }}\n*Branch:* ${{ github.ref_name }}\n*Commit:* ${{ github.sha }}"
                }
              },
              {
                "type": "actions",
                "elements": [
                  {
                    "type": "button",
                    "text": { "type": "plain_text", "text": "View Run" },
                    "url": "${{ github.server_url }}/${{ github.repository }}/actions/runs/${{ github.run_id }}"
                  }
                ]
              }
            ]
          }
      env:
        SLACK_WEBHOOK_URL: ${{ secrets.SLACK_WEBHOOK_URL }}
```

---

## 62. 워크플로우 디버깅

### Step 62.1: 디버그 로깅 활성화

```yaml
jobs:
  build:
    runs-on: ubuntu-latest
    
    steps:
    - uses: actions/checkout@v4
    
    # 디버그 정보 출력
    - name: Debug info
      run: |
        echo "Event: ${{ github.event_name }}"
        echo "Ref: ${{ github.ref }}"
        echo "SHA: ${{ github.sha }}"
        echo "Actor: ${{ github.actor }}"
        echo "Workflow: ${{ github.workflow }}"
        echo "Run ID: ${{ github.run_id }}"
        echo "Run Number: ${{ github.run_number }}"
    
    # 환경 변수 출력
    - name: Print env
      run: env | sort
    
    # 컨텍스트 출력
    - name: Dump GitHub context
      env:
        GITHUB_CONTEXT: ${{ toJSON(github) }}
      run: echo "$GITHUB_CONTEXT"
```

### Step 62.2: 저장소 시크릿으로 디버그 모드

```
┌────────────────────────────────────────────────────────────────┐
│  디버그 모드 활성화                                            │
├────────────────────────────────────────────────────────────────┤
│                                                                │
│  저장소 시크릿 추가:                                           │
│  • ACTIONS_RUNNER_DEBUG = true                                │
│  • ACTIONS_STEP_DEBUG = true                                  │
│                                                                │
│  또는 워크플로우 재실행 시:                                    │
│  "Re-run jobs" → "Enable debug logging" 체크                  │
│                                                                │
└────────────────────────────────────────────────────────────────┘
```

---

# Part 7: 실전 시나리오

---

## 63. 전체 CI/CD 파이프라인

### Step 63.1: 종합 파이프라인

```yaml
# .github/workflows/pipeline.yml
name: CI/CD Pipeline

on:
  push:
    branches: [ main, develop ]
    tags: [ 'v*' ]
  pull_request:
    branches: [ main ]

env:
  DOTNET_VERSION: '8.0.x'

jobs:
  # 1단계: 빌드 및 테스트
  build:
    runs-on: ubuntu-latest
    
    steps:
    - uses: actions/checkout@v4
    
    - name: Setup .NET
      uses: actions/setup-dotnet@v4
      with:
        dotnet-version: ${{ env.DOTNET_VERSION }}
    
    - name: Cache NuGet
      uses: actions/cache@v3
      with:
        path: ~/.nuget/packages
        key: ${{ runner.os }}-nuget-${{ hashFiles('**/*.csproj') }}
    
    - name: Restore
      run: dotnet restore
    
    - name: Build
      run: dotnet build --no-restore --configuration Release
    
    - name: Test
      run: dotnet test --no-build --configuration Release --collect:"XPlat Code Coverage"
    
    - name: Upload coverage
      uses: codecov/codecov-action@v3
      with:
        files: ./**/coverage.cobertura.xml
    
    - name: Publish
      run: dotnet publish src/MyCalculator/MyCalculator.csproj -c Release -o ./publish
    
    - name: Upload artifact
      uses: actions/upload-artifact@v3
      with:
        name: app
        path: ./publish
  
  # 2단계: 코드 품질 체크 (PR만)
  quality:
    if: github.event_name == 'pull_request'
    runs-on: ubuntu-latest
    
    steps:
    - uses: actions/checkout@v4
    
    - name: Setup .NET
      uses: actions/setup-dotnet@v4
      with:
        dotnet-version: ${{ env.DOTNET_VERSION }}
    
    - name: Format check
      run: dotnet format --verify-no-changes
    
    - name: Security scan
      run: |
        dotnet restore
        dotnet list package --vulnerable --include-transitive
  
  # 3단계: Staging 배포 (main 브랜치)
  deploy-staging:
    needs: build
    if: github.ref == 'refs/heads/main' && github.event_name == 'push'
    runs-on: ubuntu-latest
    environment: staging
    
    steps:
    - name: Download artifact
      uses: actions/download-artifact@v3
      with:
        name: app
        path: ./app
    
    - name: Deploy to Staging
      run: |
        echo "Deploying to staging..."
        ls -la ./app
  
  # 4단계: Production 배포 (태그)
  deploy-production:
    needs: build
    if: startsWith(github.ref, 'refs/tags/v')
    runs-on: ubuntu-latest
    environment: production
    
    steps:
    - uses: actions/checkout@v4
    
    - name: Download artifact
      uses: actions/download-artifact@v3
      with:
        name: app
        path: ./app
    
    - name: Deploy to Production
      run: |
        echo "Deploying to production..."
    
    - name: Create Release
      uses: softprops/action-gh-release@v1
      with:
        generate_release_notes: true
```

---

## 64. 모노레포 설정

### Step 64.1: 경로 필터

```yaml
# .github/workflows/api.yml
name: API CI

on:
  push:
    branches: [ main ]
    paths:
      - 'src/Api/**'
      - 'tests/Api.Tests/**'
      - '.github/workflows/api.yml'
  pull_request:
    branches: [ main ]
    paths:
      - 'src/Api/**'
      - 'tests/Api.Tests/**'

jobs:
  build:
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: src/Api
    
    steps:
    - uses: actions/checkout@v4
    
    - name: Setup .NET
      uses: actions/setup-dotnet@v4
      with:
        dotnet-version: '8.0.x'
    
    - name: Build API
      run: dotnet build
    
    - name: Test API
      working-directory: tests/Api.Tests
      run: dotnet test
```

---

## 65. Docker 이미지 빌드

### Step 65.1: Docker 빌드 및 푸시

```yaml
# .github/workflows/docker.yml
name: Docker

on:
  push:
    branches: [ main ]
    tags: [ 'v*' ]

env:
  REGISTRY: ghcr.io
  IMAGE_NAME: ${{ github.repository }}

jobs:
  build:
    runs-on: ubuntu-latest
    permissions:
      contents: read
      packages: write
    
    steps:
    - uses: actions/checkout@v4
    
    - name: Log in to Container Registry
      uses: docker/login-action@v3
      with:
        registry: ${{ env.REGISTRY }}
        username: ${{ github.actor }}
        password: ${{ secrets.GITHUB_TOKEN }}
    
    - name: Extract metadata
      id: meta
      uses: docker/metadata-action@v5
      with:
        images: ${{ env.REGISTRY }}/${{ env.IMAGE_NAME }}
        tags: |
          type=ref,event=branch
          type=semver,pattern={{version}}
          type=sha
    
    - name: Build and push
      uses: docker/build-push-action@v5
      with:
        context: .
        push: true
        tags: ${{ steps.meta.outputs.tags }}
        labels: ${{ steps.meta.outputs.labels }}
```

### Step 65.2: Dockerfile 예시

```dockerfile
# Dockerfile
FROM mcr.microsoft.com/dotnet/sdk:8.0 AS build
WORKDIR /src

COPY *.sln .
COPY src/*/*.csproj ./src/
RUN for file in src/*.csproj; do \
      dir=$(dirname $file); \
      mkdir -p $dir; \
      mv $file $dir/; \
    done
RUN dotnet restore

COPY . .
RUN dotnet publish src/MyCalculator/MyCalculator.csproj -c Release -o /app

FROM mcr.microsoft.com/dotnet/runtime:8.0
WORKDIR /app
COPY --from=build /app .
ENTRYPOINT ["dotnet", "MyCalculator.dll"]
```

---

## 66. NuGet 패키지 배포

### Step 66.1: NuGet 배포 워크플로우

```yaml
# .github/workflows/nuget.yml
name: NuGet

on:
  release:
    types: [ published ]

jobs:
  publish:
    runs-on: ubuntu-latest
    
    steps:
    - uses: actions/checkout@v4
    
    - name: Setup .NET
      uses: actions/setup-dotnet@v4
      with:
        dotnet-version: '8.0.x'
    
    - name: Get version
      id: version
      run: echo "VERSION=${GITHUB_REF#refs/tags/v}" >> $GITHUB_OUTPUT
    
    - name: Build
      run: dotnet build --configuration Release /p:Version=${{ steps.version.outputs.VERSION }}
    
    - name: Test
      run: dotnet test --configuration Release --no-build
    
    - name: Pack
      run: dotnet pack --configuration Release --no-build -o ./nupkg /p:Version=${{ steps.version.outputs.VERSION }}
    
    - name: Push to NuGet
      run: |
        dotnet nuget push ./nupkg/*.nupkg \
          --api-key ${{ secrets.NUGET_API_KEY }} \
          --source https://api.nuget.org/v3/index.json \
          --skip-duplicate
```

---

## 67. Azure 배포

### Step 67.1: Azure Web App 배포

```yaml
# .github/workflows/azure.yml
name: Azure Deploy

on:
  push:
    branches: [ main ]

jobs:
  build-and-deploy:
    runs-on: ubuntu-latest
    
    steps:
    - uses: actions/checkout@v4
    
    - name: Setup .NET
      uses: actions/setup-dotnet@v4
      with:
        dotnet-version: '8.0.x'
    
    - name: Build
      run: dotnet build --configuration Release
    
    - name: Publish
      run: dotnet publish -c Release -o ./publish
    
    - name: Deploy to Azure
      uses: azure/webapps-deploy@v2
      with:
        app-name: 'my-app-name'
        publish-profile: ${{ secrets.AZURE_WEBAPP_PUBLISH_PROFILE }}
        package: ./publish
```

---

## 68. AWS 배포

### Step 68.1: AWS Elastic Beanstalk 배포

```yaml
# .github/workflows/aws.yml
name: AWS Deploy

on:
  push:
    branches: [ main ]

jobs:
  deploy:
    runs-on: ubuntu-latest
    
    steps:
    - uses: actions/checkout@v4
    
    - name: Setup .NET
      uses: actions/setup-dotnet@v4
      with:
        dotnet-version: '8.0.x'
    
    - name: Build and Publish
      run: |
        dotnet publish -c Release -o ./publish
        cd publish && zip -r ../deploy.zip .
    
    - name: Configure AWS credentials
      uses: aws-actions/configure-aws-credentials@v4
      with:
        aws-access-key-id: ${{ secrets.AWS_ACCESS_KEY_ID }}
        aws-secret-access-key: ${{ secrets.AWS_SECRET_ACCESS_KEY }}
        aws-region: ap-northeast-2
    
    - name: Deploy to Elastic Beanstalk
      run: |
        aws s3 cp deploy.zip s3://my-bucket/deploy.zip
        aws elasticbeanstalk create-application-version \
          --application-name my-app \
          --version-label ${{ github.sha }} \
          --source-bundle S3Bucket=my-bucket,S3Key=deploy.zip
        aws elasticbeanstalk update-environment \
          --environment-name my-env \
          --version-label ${{ github.sha }}
```

---

## 69. 테스트 리포트 통합

### Step 69.1: 상세 테스트 리포트

```yaml
# .github/workflows/test-report.yml
name: Test Report

on:
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]

jobs:
  test:
    runs-on: ubuntu-latest
    
    steps:
    - uses: actions/checkout@v4
    
    - name: Setup .NET
      uses: actions/setup-dotnet@v4
      with:
        dotnet-version: '8.0.x'
    
    - name: Test
      run: |
        dotnet test \
          --logger "trx;LogFileName=test-results.trx" \
          --logger "html;LogFileName=test-results.html" \
          --collect:"XPlat Code Coverage" \
          --results-directory ./TestResults
    
    # TRX 리포트
    - name: Test Report
      uses: dorny/test-reporter@v1
      if: always()
      with:
        name: .NET Tests
        path: '**/test-results.trx'
        reporter: dotnet-trx
    
    # HTML 리포트 업로드
    - name: Upload HTML Report
      uses: actions/upload-artifact@v3
      if: always()
      with:
        name: test-report
        path: TestResults/test-results.html
    
    # 커버리지 업로드
    - name: Upload Coverage
      uses: codecov/codecov-action@v3
```

---

## 70. 성능 테스트

### Step 70.1: 벤치마크 실행

```yaml
# .github/workflows/benchmark.yml
name: Benchmark

on:
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]

jobs:
  benchmark:
    runs-on: ubuntu-latest
    
    steps:
    - uses: actions/checkout@v4
    
    - name: Setup .NET
      uses: actions/setup-dotnet@v4
      with:
        dotnet-version: '8.0.x'
    
    - name: Run benchmarks
      run: |
        dotnet run --project tests/MyCalculator.Benchmarks \
          --configuration Release \
          -- --export json
    
    - name: Upload benchmark results
      uses: actions/upload-artifact@v3
      with:
        name: benchmark-results
        path: BenchmarkDotNet.Artifacts/
    
    # 벤치마크 결과 비교 (PR)
    - name: Compare benchmarks
      if: github.event_name == 'pull_request'
      uses: benchmark-action/github-action-benchmark@v1
      with:
        tool: 'benchmarkdotnet'
        output-file-path: BenchmarkDotNet.Artifacts/results.json
        github-token: ${{ secrets.GITHUB_TOKEN }}
        comment-on-alert: true
        alert-threshold: '150%'
```

---

## 71. E2E 테스트

### Step 71.1: Playwright 테스트

```yaml
# .github/workflows/e2e.yml
name: E2E Tests

on:
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]

jobs:
  e2e:
    runs-on: ubuntu-latest
    
    steps:
    - uses: actions/checkout@v4
    
    - name: Setup .NET
      uses: actions/setup-dotnet@v4
      with:
        dotnet-version: '8.0.x'
    
    - name: Build
      run: dotnet build
    
    - name: Install Playwright
      run: |
        dotnet tool install --global Microsoft.Playwright.CLI
        playwright install --with-deps
    
    - name: Run E2E tests
      run: dotnet test tests/MyApp.E2E.Tests
    
    - name: Upload test results
      uses: actions/upload-artifact@v3
      if: always()
      with:
        name: playwright-report
        path: tests/MyApp.E2E.Tests/playwright-report/
```

---

## 72. 워크플로우 파일 모음

### Step 72.1: 파일 구조

```
┌────────────────────────────────────────────────────────────────┐
│  .github/workflows/ 디렉토리 구조                              │
├────────────────────────────────────────────────────────────────┤
│                                                                │
│  .github/                                                      │
│  ├── workflows/                                                │
│  │   ├── ci.yml              # 기본 CI                        │
│  │   ├── pr-check.yml        # PR 체크                        │
│  │   ├── coverage.yml        # 커버리지                       │
│  │   ├── quality.yml         # 코드 품질                      │
│  │   ├── release.yml         # 릴리스                         │
│  │   ├── deploy.yml          # 배포                           │
│  │   ├── scheduled.yml       # 스케줄 작업                    │
│  │   └── docker.yml          # Docker 빌드                    │
│  │                                                             │
│  ├── actions/                 # 커스텀 액션                    │
│  │   └── dotnet-setup/                                         │
│  │       └── action.yml                                        │
│  │                                                             │
│  ├── dependabot.yml          # Dependabot 설정                │
│  ├── labeler.yml             # PR 라벨러 설정                 │
│  └── CODEOWNERS              # 코드 오너                      │
│                                                                │
└────────────────────────────────────────────────────────────────┘
```

---

# Part 8: 정리 및 베스트 프랙티스

---

## 73. 학습 체크리스트

### Step 73.1: 완료 항목 확인

```
┌────────────────────────────────────────────────────────────────┐
│  ✅ 학습 완료 체크리스트                                       │
├────────────────────────────────────────────────────────────────┤
│                                                                │
│  CI/CD 개념                                                    │
│  ☑ CI/CD 정의와 장점 이해                                     │
│  ☑ 파이프라인 구조 이해                                       │
│  ☑ GitHub Actions 핵심 용어                                   │
│                                                                │
│  기본 워크플로우                                               │
│  ☑ YAML 문법                                                  │
│  ☑ 이벤트 트리거                                              │
│  ☑ 러너 설정                                                  │
│  ☑ 스텝과 액션                                                │
│                                                                │
│  .NET CI 구성                                                  │
│  ☑ dotnet 명령어 사용                                         │
│  ☑ 캐싱 설정                                                  │
│  ☑ 매트릭스 빌드                                              │
│  ☑ 아티팩트 관리                                              │
│                                                                │
│  PR 자동화                                                     │
│  ☑ PR 트리거 설정                                             │
│  ☑ 브랜치 보호 규칙                                           │
│  ☑ 상태 체크                                                  │
│                                                                │
│  코드 품질                                                     │
│  ☑ 커버리지 측정                                              │
│  ☑ 코드 포맷팅 체크                                           │
│  ☑ 보안 스캔                                                  │
│                                                                │
│  고급 기능                                                     │
│  ☑ 재사용 가능 워크플로우                                     │
│  ☑ 환경(Environment) 설정                                     │
│  ☑ 시크릿 관리                                                │
│  ☑ 릴리스 자동화                                              │
│                                                                │
└────────────────────────────────────────────────────────────────┘
```

---

## 74. 베스트 프랙티스

### Step 74.1: 워크플로우 작성 가이드

```
┌────────────────────────────────────────────────────────────────┐
│  GitHub Actions 베스트 프랙티스                                │
├────────────────────────────────────────────────────────────────┤
│                                                                │
│  1. 빠른 피드백                                                │
│  ─────────────────────────────────────────────────────────────│
│  • 캐싱 적극 활용                                             │
│  • 병렬 작업 사용                                             │
│  • 불필요한 스텝 제거                                         │
│  • 타임아웃 설정                                              │
│                                                                │
│  2. 안정성                                                     │
│  ─────────────────────────────────────────────────────────────│
│  • 액션 버전 고정 (v4, @sha)                                  │
│  • 실패 시 알림 설정                                          │
│  • 재시도 로직 추가                                           │
│  • 동시성 제어                                                │
│                                                                │
│  3. 보안                                                       │
│  ─────────────────────────────────────────────────────────────│
│  • 시크릿 사용 (하드코딩 금지)                                │
│  • 최소 권한 원칙                                             │
│  • Dependabot 활성화                                          │
│  • 취약점 스캔                                                │
│                                                                │
│  4. 유지보수                                                   │
│  ─────────────────────────────────────────────────────────────│
│  • 재사용 가능한 워크플로우                                   │
│  • 환경 변수 활용                                             │
│  • 명확한 워크플로우 이름                                     │
│  • 주석 추가                                                  │
│                                                                │
└────────────────────────────────────────────────────────────────┘
```

---

## 75. 흔한 실수와 해결

### Step 75.1: 트러블슈팅

```
┌────────────────────────────────────────────────────────────────┐
│  흔한 실수와 해결 방법                                         │
├────────────────────────────────────────────────────────────────┤
│                                                                │
│  문제 1: 워크플로우가 트리거되지 않음                          │
│  ─────────────────────────────────────────────────────────────│
│  • .github/workflows/ 경로 확인                               │
│  • on: 이벤트 설정 확인                                       │
│  • 브랜치 이름 확인                                           │
│  • YAML 문법 오류 확인                                        │
│                                                                │
│  문제 2: 권한 오류                                             │
│  ─────────────────────────────────────────────────────────────│
│  • permissions 섹션 추가                                      │
│  • GITHUB_TOKEN 권한 확인                                     │
│  • 저장소 설정에서 Actions 권한 확인                          │
│                                                                │
│  문제 3: 캐시가 작동하지 않음                                  │
│  ─────────────────────────────────────────────────────────────│
│  • 캐시 키 확인                                               │
│  • 경로 확인                                                  │
│  • 복원 키(restore-keys) 설정                                 │
│                                                                │
│  문제 4: 시크릿이 비어있음                                     │
│  ─────────────────────────────────────────────────────────────│
│  • Fork에서는 시크릿 접근 불가                                │
│  • 환경 시크릿 vs 저장소 시크릿 확인                          │
│  • 시크릿 이름 대소문자 확인                                  │
│                                                                │
│  문제 5: 워크플로우가 너무 느림                                │
│  ─────────────────────────────────────────────────────────────│
│  • 캐싱 추가                                                  │
│  • 병렬 작업 사용                                             │
│  • 불필요한 스텝 제거                                         │
│  • 조건부 실행 활용                                           │
│                                                                │
└────────────────────────────────────────────────────────────────┘
```

---

## 76. 주요 명령어 요약

### Step 76.1: dotnet 명령어

| 명령어 | 설명 |
|--------|------|
| `dotnet restore` | 패키지 복원 |
| `dotnet build` | 빌드 |
| `dotnet test` | 테스트 실행 |
| `dotnet publish` | 배포용 빌드 |
| `dotnet pack` | NuGet 패키지 생성 |
| `dotnet format` | 코드 포맷팅 |

### Step 76.2: GitHub Actions 문법

| 문법 | 설명 |
|------|------|
| `on:` | 이벤트 트리거 |
| `jobs:` | 작업 정의 |
| `runs-on:` | 러너 지정 |
| `steps:` | 스텝 정의 |
| `uses:` | 액션 사용 |
| `run:` | 명령어 실행 |
| `if:` | 조건부 실행 |
| `needs:` | 작업 의존성 |
| `env:` | 환경 변수 |
| `secrets.` | 시크릿 접근 |

---

## 77. 유용한 액션 목록

### Step 77.1: 자주 사용하는 액션

```
┌────────────────────────────────────────────────────────────────┐
│  자주 사용하는 GitHub Actions                                  │
├────────────────────────────────────────────────────────────────┤
│                                                                │
│  기본                                                          │
│  • actions/checkout@v4          코드 체크아웃                 │
│  • actions/setup-dotnet@v4      .NET 설정                     │
│  • actions/cache@v3             캐싱                          │
│  • actions/upload-artifact@v3   아티팩트 업로드               │
│  • actions/download-artifact@v3 아티팩트 다운로드             │
│                                                                │
│  테스트/품질                                                   │
│  • codecov/codecov-action@v3    Codecov 업로드               │
│  • dorny/test-reporter@v1       테스트 리포트                 │
│                                                                │
│  배포                                                          │
│  • azure/webapps-deploy@v2      Azure 배포                    │
│  • aws-actions/configure-aws-credentials@v4  AWS 인증         │
│  • docker/build-push-action@v5  Docker 빌드/푸시             │
│                                                                │
│  알림/유틸                                                     │
│  • slackapi/slack-github-action@v1  Slack 알림               │
│  • softprops/action-gh-release@v1   릴리스 생성              │
│  • actions/labeler@v4           PR 라벨링                     │
│                                                                │
└────────────────────────────────────────────────────────────────┘
```

---

## 78. 추가 학습 자료

### Step 78.1: 참고 링크

```
┌────────────────────────────────────────────────────────────────┐
│  추가 학습 자료                                                │
├────────────────────────────────────────────────────────────────┤
│                                                                │
│  공식 문서                                                     │
│  • GitHub Actions: https://docs.github.com/actions            │
│  • .NET CLI: https://docs.microsoft.com/dotnet/core/tools     │
│  • Coverlet: https://github.com/coverlet-coverage/coverlet    │
│                                                                │
│  마켓플레이스                                                  │
│  • https://github.com/marketplace?type=actions                │
│                                                                │
│  예제 저장소                                                   │
│  • https://github.com/actions/starter-workflows               │
│                                                                │
└────────────────────────────────────────────────────────────────┘
```

---

## 79. 다음 단계

### Step 79.1: 학습 로드맵

```
┌────────────────────────────────────────────────────────────────┐
│  🎯 다음 단계 학습 권장                                        │
├────────────────────────────────────────────────────────────────┤
│                                                                │
│  1. 고급 배포                                                  │
│     • Kubernetes 배포                                         │
│     • Blue-Green / Canary 배포                                │
│     • GitOps (ArgoCD, Flux)                                   │
│                                                                │
│  2. 인프라 자동화                                              │
│     • Terraform                                               │
│     • Pulumi                                                  │
│     • Infrastructure as Code                                  │
│                                                                │
│  3. 모니터링                                                   │
│     • Application Insights                                    │
│     • Prometheus + Grafana                                    │
│     • 로그 수집 및 분석                                       │
│                                                                │
│  4. 보안 강화                                                  │
│     • SAST/DAST                                               │
│     • 컨테이너 보안 스캔                                      │
│     • 시크릿 관리 (HashiCorp Vault)                           │
│                                                                │
└────────────────────────────────────────────────────────────────┘
```

---

## 80. 요약

### Step 80.1: 핵심 내용 요약

```
┌────────────────────────────────────────────────────────────────┐
│  🚀 CI/CD 핵심 요약                                            │
├────────────────────────────────────────────────────────────────┤
│                                                                │
│  CI/CD란?                                                      │
│  • CI: 자동 빌드 + 테스트                                     │
│  • CD: 자동 배포                                              │
│                                                                │
│  GitHub Actions 기본:                                          │
│  • .github/workflows/*.yml                                    │
│  • on: 이벤트 → jobs: 작업 → steps: 스텝                     │
│                                                                │
│  필수 설정:                                                    │
│  • PR 자동 테스트                                             │
│  • 브랜치 보호 규칙                                           │
│  • 커버리지 측정                                              │
│                                                                │
│  최적화:                                                       │
│  • 캐싱으로 속도 향상                                         │
│  • 매트릭스로 다중 환경 테스트                                │
│  • 동시성 제어                                                │
│                                                                │
│  보안:                                                         │
│  • 시크릿 사용                                                │
│  • 환경 보호 규칙                                             │
│  • Dependabot 활성화                                          │
│                                                                │
└────────────────────────────────────────────────────────────────┘
```

---

# 🎉 축하합니다!

CI/CD 통합 실습을 완료했습니다!

이제 여러분은:
- **GitHub Actions**로 CI/CD 파이프라인을 구축할 수 있습니다
- **PR 자동 테스트**를 설정할 수 있습니다
- **브랜치 보호 규칙**으로 코드 품질을 관리할 수 있습니다
- **커버리지, 보안 스캔** 등 품질 게이트를 구성할 수 있습니다
- **자동 배포** 파이프라인을 만들 수 있습니다

프로젝트에 CI/CD를 적용하여 개발 생산성을 높이세요!

---

**수고하셨습니다! 🎉**
