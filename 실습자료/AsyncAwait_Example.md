# 🔄 C# async/await 비동기 처리 예제

## 대기 시간 동안 다른 일을 하는 방법

---

## 1. 동기 vs 비동기 비교

### 예제 코드

```csharp
using System;
using System.Threading.Tasks;

namespace AsyncAwaitDemo
{
    class Program
    {
        static async Task Main(string[] args)
        {
            Console.WriteLine("=== 동기 방식 ===");
            SyncExample();
            
            Console.WriteLine("\n=== 비동기 방식 ===");
            await AsyncExample();
        }
        
        // ❌ 동기 방식: 순차적으로 대기 (총 6초)
        static void SyncExample()
        {
            var startTime = DateTime.Now;
            
            Console.WriteLine("커피 주문 시작...");
            MakeCoffeeSync();      // 3초 대기
            
            Console.WriteLine("토스트 주문 시작...");
            MakeToastSync();       // 3초 대기
            
            var elapsed = (DateTime.Now - startTime).TotalSeconds;
            Console.WriteLine($"완료! 총 소요 시간: {elapsed:F1}초");
        }
        
        // ✅ 비동기 방식: 동시에 처리 (총 3초)
        static async Task AsyncExample()
        {
            var startTime = DateTime.Now;
            
            Console.WriteLine("커피 주문 시작...");
            var coffeeTask = MakeCoffeeAsync();  // 시작만 하고 기다리지 않음
            
            Console.WriteLine("토스트 주문 시작...");
            var toastTask = MakeToastAsync();    // 시작만 하고 기다리지 않음
            
            Console.WriteLine("두 작업이 동시에 진행 중...");
            
            // 두 작업이 모두 완료될 때까지 대기
            await Task.WhenAll(coffeeTask, toastTask);
            
            var elapsed = (DateTime.Now - startTime).TotalSeconds;
            Console.WriteLine($"완료! 총 소요 시간: {elapsed:F1}초");
        }
        
        // 동기 메서드
        static void MakeCoffeeSync()
        {
            System.Threading.Thread.Sleep(3000);  // 3초 블로킹
            Console.WriteLine("☕ 커피 완성!");
        }
        
        static void MakeToastSync()
        {
            System.Threading.Thread.Sleep(3000);  // 3초 블로킹
            Console.WriteLine("🍞 토스트 완성!");
        }
        
        // 비동기 메서드
        static async Task MakeCoffeeAsync()
        {
            await Task.Delay(3000);  // 3초 대기 (논블로킹)
            Console.WriteLine("☕ 커피 완성!");
        }
        
        static async Task MakeToastAsync()
        {
            await Task.Delay(3000);  // 3초 대기 (논블로킹)
            Console.WriteLine("🍞 토스트 완성!");
        }
    }
}
```

### 실행 결과

```
=== 동기 방식 ===
커피 주문 시작...
☕ 커피 완성!
토스트 주문 시작...
🍞 토스트 완성!
완료! 총 소요 시간: 6.0초

=== 비동기 방식 ===
커피 주문 시작...
토스트 주문 시작...
두 작업이 동시에 진행 중...
☕ 커피 완성!
🍞 토스트 완성!
완료! 총 소요 시간: 3.0초
```

### 비교 다이어그램

```
┌────────────────────────────────────────────────────────────────┐
│  동기 방식 (Synchronous)                                       │
├────────────────────────────────────────────────────────────────┤
│                                                                │
│  시간 →                                                        │
│  0초        3초        6초                                     │
│  ├─────────┼─────────┤                                        │
│  │  커피   │  토스트  │                                        │
│  │  제조   │  제조    │                                        │
│  ├─────────┼─────────┤                                        │
│                                                                │
│  총 소요 시간: 6초 (3초 + 3초)                                │
│                                                                │
└────────────────────────────────────────────────────────────────┘

┌────────────────────────────────────────────────────────────────┐
│  비동기 방식 (Asynchronous)                                    │
├────────────────────────────────────────────────────────────────┤
│                                                                │
│  시간 →                                                        │
│  0초              3초                                          │
│  ├───────────────┤                                            │
│  │  커피 제조     │                                            │
│  ├───────────────┤                                            │
│  │  토스트 제조   │  ← 동시 진행!                              │
│  ├───────────────┤                                            │
│                                                                │
│  총 소요 시간: 3초 (동시 실행)                                │
│                                                                │
└────────────────────────────────────────────────────────────────┘
```

---

## 2. 대기 시간 동안 다른 일 하기

### 예제 코드

```csharp
using System;
using System.Threading.Tasks;

namespace AsyncAwaitDemo
{
    class Program
    {
        static async Task Main(string[] args)
        {
            Console.WriteLine("=== 대기 시간 동안 다른 일 하기 ===\n");
            
            // 파일 다운로드 시작 (시간이 오래 걸리는 작업)
            Console.WriteLine("[1] 대용량 파일 다운로드 시작...");
            var downloadTask = DownloadFileAsync();
            
            // 다운로드 대기 중에 다른 작업 수행
            Console.WriteLine("[2] 다운로드 중... 다른 작업 시작!");
            
            for (int i = 1; i <= 5; i++)
            {
                Console.WriteLine($"    → 다른 작업 진행 중... ({i}/5)");
                await Task.Delay(500);  // 0.5초마다 진행 상황 표시
            }
            
            Console.WriteLine("[3] 다른 작업 완료! 다운로드 완료 대기...");
            
            // 다운로드 완료 대기
            var result = await downloadTask;
            
            Console.WriteLine($"[4] {result}");
            Console.WriteLine("\n모든 작업 완료!");
        }
        
        static async Task<string> DownloadFileAsync()
        {
            // 파일 다운로드 시뮬레이션 (3초 소요)
            await Task.Delay(3000);
            return "📁 파일 다운로드 완료! (100MB)";
        }
    }
}
```

### 실행 결과

```
=== 대기 시간 동안 다른 일 하기 ===

[1] 대용량 파일 다운로드 시작...
[2] 다운로드 중... 다른 작업 시작!
    → 다른 작업 진행 중... (1/5)
    → 다른 작업 진행 중... (2/5)
    → 다른 작업 진행 중... (3/5)
    → 다른 작업 진행 중... (4/5)
    → 다른 작업 진행 중... (5/5)
[3] 다른 작업 완료! 다운로드 완료 대기...
[4] 📁 파일 다운로드 완료! (100MB)

모든 작업 완료!
```

### 실행 흐름 다이어그램

```
┌────────────────────────────────────────────────────────────────┐
│  대기 시간 동안 다른 일 하기                                   │
├────────────────────────────────────────────────────────────────┤
│                                                                │
│  시간 →                                                        │
│  0초     0.5초    1초     1.5초    2초     2.5초    3초       │
│  ├───────┼───────┼───────┼───────┼───────┼───────┤           │
│  │                                                │           │
│  │  ┌─────────── 다운로드 (3초) ──────────────┐  │           │
│  │  │         백그라운드에서 진행              │  │           │
│  │  └─────────────────────────────────────────┘  │           │
│  │                                                │           │
│  │  ┌───┐ ┌───┐ ┌───┐ ┌───┐ ┌───┐               │           │
│  │  │1/5│ │2/5│ │3/5│ │4/5│ │5/5│  다른 작업    │           │
│  │  └───┘ └───┘ └───┘ └───┘ └───┘               │           │
│  │                                                │           │
│  ├───────────────────────────────────────────────┤           │
│                                                                │
│  핵심: 다운로드 대기 중에도 다른 작업 수행 가능!              │
│                                                                │
└────────────────────────────────────────────────────────────────┘
```

---

## 3. 실용적인 예제: 여러 API 동시 호출

### 예제 코드

```csharp
using System;
using System.Collections.Generic;
using System.Threading.Tasks;

namespace AsyncAwaitDemo
{
    class Program
    {
        static async Task Main(string[] args)
        {
            Console.WriteLine("=== 여러 API 동시 호출 예제 ===\n");
            
            var startTime = DateTime.Now;
            
            // 방법 1: 순차 호출 (느림)
            Console.WriteLine("--- 순차 호출 ---");
            var seq_start = DateTime.Now;
            
            var user = await GetUserAsync(1);
            var orders = await GetOrdersAsync(1);
            var products = await GetProductsAsync();
            
            Console.WriteLine($"순차 호출 소요 시간: {(DateTime.Now - seq_start).TotalSeconds:F1}초\n");
            
            // 방법 2: 동시 호출 (빠름)
            Console.WriteLine("--- 동시 호출 ---");
            var par_start = DateTime.Now;
            
            var userTask = GetUserAsync(1);
            var ordersTask = GetOrdersAsync(1);
            var productsTask = GetProductsAsync();
            
            // 세 작업이 동시에 진행됨
            await Task.WhenAll(userTask, ordersTask, productsTask);
            
            // 결과 가져오기
            var user2 = userTask.Result;
            var orders2 = ordersTask.Result;
            var products2 = productsTask.Result;
            
            Console.WriteLine($"동시 호출 소요 시간: {(DateTime.Now - par_start).TotalSeconds:F1}초");
        }
        
        // 사용자 정보 조회 (1초 소요)
        static async Task<string> GetUserAsync(int userId)
        {
            Console.WriteLine("  👤 사용자 정보 요청...");
            await Task.Delay(1000);
            Console.WriteLine("  👤 사용자 정보 완료!");
            return $"User_{userId}";
        }
        
        // 주문 목록 조회 (2초 소요)
        static async Task<List<string>> GetOrdersAsync(int userId)
        {
            Console.WriteLine("  📦 주문 목록 요청...");
            await Task.Delay(2000);
            Console.WriteLine("  📦 주문 목록 완료!");
            return new List<string> { "Order1", "Order2" };
        }
        
        // 상품 목록 조회 (1.5초 소요)
        static async Task<List<string>> GetProductsAsync()
        {
            Console.WriteLine("  🛒 상품 목록 요청...");
            await Task.Delay(1500);
            Console.WriteLine("  🛒 상품 목록 완료!");
            return new List<string> { "Product1", "Product2", "Product3" };
        }
    }
}
```

### 실행 결과

```
=== 여러 API 동시 호출 예제 ===

--- 순차 호출 ---
  👤 사용자 정보 요청...
  👤 사용자 정보 완료!
  📦 주문 목록 요청...
  📦 주문 목록 완료!
  🛒 상품 목록 요청...
  🛒 상품 목록 완료!
순차 호출 소요 시간: 4.5초

--- 동시 호출 ---
  👤 사용자 정보 요청...
  📦 주문 목록 요청...
  🛒 상품 목록 요청...
  👤 사용자 정보 완료!
  🛒 상품 목록 완료!
  📦 주문 목록 완료!
동시 호출 소요 시간: 2.0초
```

### 시간 비교 다이어그램

```
┌────────────────────────────────────────────────────────────────┐
│  순차 호출 vs 동시 호출                                        │
├────────────────────────────────────────────────────────────────┤
│                                                                │
│  순차 호출 (Sequential):                                       │
│  시간 →                                                        │
│  0초     1초     2초     3초     4초     4.5초                │
│  ├──────┼───────┼───────┼───────┼──────┤                     │
│  │ User │      Orders      │  Products  │                     │
│  │ 1초  │       2초        │    1.5초   │                     │
│  ├──────┴──────────────────┴────────────┤                     │
│                                                                │
│  총 소요 시간: 4.5초 (1 + 2 + 1.5)                            │
│                                                                │
│  ─────────────────────────────────────────────────────────────│
│                                                                │
│  동시 호출 (Parallel):                                         │
│  시간 →                                                        │
│  0초     1초     1.5초   2초                                  │
│  ├──────┼───────┼───────┤                                     │
│  │ User │                                                      │
│  ├──────┤                                                      │
│  │   Products   │                                              │
│  ├──────────────┤                                              │
│  │      Orders        │  ← 가장 긴 작업 기준                  │
│  ├────────────────────┤                                        │
│                                                                │
│  총 소요 시간: 2초 (가장 긴 작업 = Orders 2초)                │
│                                                                │
│  ⚡ 성능 향상: 4.5초 → 2초 (55% 단축!)                        │
│                                                                │
└────────────────────────────────────────────────────────────────┘
```

---

## 4. 진행률 표시 예제

### 예제 코드

```csharp
using System;
using System.Threading.Tasks;

namespace AsyncAwaitDemo
{
    class Program
    {
        static async Task Main(string[] args)
        {
            Console.WriteLine("=== 진행률 표시 예제 ===\n");
            
            // Progress<T>를 사용하여 진행률 보고
            var progress = new Progress<int>(percent =>
            {
                Console.Write($"\r다운로드 진행률: [{new string('█', percent / 5)}{new string('░', 20 - percent / 5)}] {percent}%");
            });
            
            await DownloadWithProgressAsync(progress);
            
            Console.WriteLine("\n\n다운로드 완료!");
        }
        
        static async Task DownloadWithProgressAsync(IProgress<int> progress)
        {
            for (int i = 0; i <= 100; i += 5)
            {
                await Task.Delay(200);  // 다운로드 시뮬레이션
                progress.Report(i);
            }
        }
    }
}
```

### 실행 결과

```
=== 진행률 표시 예제 ===

다운로드 진행률: [████████████████████] 100%

다운로드 완료!
```

---

## 5. VS 2017 호환 버전 (.NET Framework 4.7.2)

### 예제 코드

```csharp
using System;
using System.Collections.Generic;
using System.Threading.Tasks;

namespace AsyncAwaitDemo
{
    class Program
    {
        // VS 2017에서는 async Main을 직접 사용할 수 없으므로
        // 동기 Main에서 비동기 메서드 호출
        static void Main(string[] args)
        {
            Console.WriteLine("=== async/await 예제 (VS 2017) ===\n");
            
            // 비동기 메서드를 동기적으로 실행
            RunAsync().GetAwaiter().GetResult();
            
            Console.WriteLine("\n프로그램 종료. 아무 키나 누르세요...");
            Console.ReadKey();
        }
        
        static async Task RunAsync()
        {
            // 예제 1: 대기 시간 동안 다른 일 하기
            Console.WriteLine("[예제 1] 대기 시간 동안 다른 일 하기\n");
            
            Console.WriteLine("파일 다운로드 시작...");
            var downloadTask = DownloadFileAsync();
            
            // 다운로드 중 다른 작업 수행
            for (int i = 1; i <= 5; i++)
            {
                Console.WriteLine(string.Format("  다른 작업 진행 중... ({0}/5)", i));
                await Task.Delay(400);
            }
            
            var result = await downloadTask;
            Console.WriteLine(result);
            
            // 예제 2: 여러 작업 동시 실행
            Console.WriteLine("\n[예제 2] 여러 작업 동시 실행\n");
            
            var startTime = DateTime.Now;
            
            // 세 작업을 동시에 시작
            var task1 = DoWorkAsync("작업 A", 1000);
            var task2 = DoWorkAsync("작업 B", 1500);
            var task3 = DoWorkAsync("작업 C", 800);
            
            // 모든 작업 완료 대기
            await Task.WhenAll(task1, task2, task3);
            
            var elapsed = (DateTime.Now - startTime).TotalSeconds;
            Console.WriteLine(string.Format("\n모든 작업 완료! 소요 시간: {0:F1}초", elapsed));
            Console.WriteLine("(순차 실행 시 3.3초, 동시 실행으로 1.5초!)");
        }
        
        static async Task<string> DownloadFileAsync()
        {
            await Task.Delay(2000);  // 2초 소요
            return "파일 다운로드 완료!";
        }
        
        static async Task DoWorkAsync(string name, int milliseconds)
        {
            Console.WriteLine(string.Format("  {0} 시작...", name));
            await Task.Delay(milliseconds);
            Console.WriteLine(string.Format("  {0} 완료! ({1}ms)", name, milliseconds));
        }
    }
}
```

### 실행 결과

```
=== async/await 예제 (VS 2017) ===

[예제 1] 대기 시간 동안 다른 일 하기

파일 다운로드 시작...
  다른 작업 진행 중... (1/5)
  다른 작업 진행 중... (2/5)
  다른 작업 진행 중... (3/5)
  다른 작업 진행 중... (4/5)
  다른 작업 진행 중... (5/5)
파일 다운로드 완료!

[예제 2] 여러 작업 동시 실행

  작업 A 시작...
  작업 B 시작...
  작업 C 시작...
  작업 C 완료! (800ms)
  작업 A 완료! (1000ms)
  작업 B 완료! (1500ms)

모든 작업 완료! 소요 시간: 1.5초
(순차 실행 시 3.3초, 동시 실행으로 1.5초!)

프로그램 종료. 아무 키나 누르세요...
```

---

## 6. 핵심 개념 요약

### async/await 기본 개념

```
┌────────────────────────────────────────────────────────────────┐
│  async/await 핵심 개념                                         │
├────────────────────────────────────────────────────────────────┤
│                                                                │
│  1. async 키워드                                               │
│     • 메서드가 비동기임을 표시                                │
│     • 반환 타입: Task, Task<T>, void                          │
│                                                                │
│  2. await 키워드                                               │
│     • 비동기 작업 완료를 기다림                               │
│     • 대기 중 스레드를 블로킹하지 않음                        │
│     • async 메서드 내에서만 사용 가능                         │
│                                                                │
│  3. Task vs Task<T>                                            │
│     • Task: 반환값 없음 (void 역할)                           │
│     • Task<T>: T 타입 반환값 있음                             │
│                                                                │
│  4. 동시 실행 패턴                                             │
│     • Task.WhenAll(): 모든 작업 완료 대기                     │
│     • Task.WhenAny(): 하나라도 완료되면 진행                  │
│                                                                │
│  5. Thread.Sleep vs Task.Delay                                 │
│     • Thread.Sleep(1000): 스레드 블로킹 (동기)               │
│     • await Task.Delay(1000): 논블로킹 (비동기)              │
│                                                                │
└────────────────────────────────────────────────────────────────┘
```

### 시간 비교

```
┌────────────────────────────────────────────────────────────────┐
│  시간 비교                                                     │
├────────────────────────────────────────────────────────────────┤
│                                                                │
│  작업 A: 2초, 작업 B: 3초, 작업 C: 1초                        │
│                                                                │
│  순차 실행:                                                    │
│  ├─ A ─┤├── B ──┤├ C ┤                                        │
│  0     2        5     6초 (총 6초)                            │
│                                                                │
│  동시 실행:                                                    │
│  ├─ A ─┤                                                       │
│  ├── B ──┤                                                     │
│  ├ C ┤                                                         │
│  0        3초 (총 3초, 가장 긴 작업 기준)                     │
│                                                                │
└────────────────────────────────────────────────────────────────┘
```

---

## 7. 자주 사용하는 패턴

### 패턴 1: 작업 시작 후 나중에 결과 받기

```csharp
// 1. 작업 시작 (await 없이)
var task = LongRunningOperationAsync();

// 2. 다른 작업 수행
DoOtherWork();

// 3. 결과 필요할 때 await
var result = await task;
```

### 패턴 2: 여러 작업 동시 실행

```csharp
// 모든 작업을 먼저 시작
var task1 = Operation1Async();
var task2 = Operation2Async();
var task3 = Operation3Async();

// 모두 완료될 때까지 대기
await Task.WhenAll(task1, task2, task3);

// 결과 사용
var result1 = task1.Result;
var result2 = task2.Result;
var result3 = task3.Result;
```

### 패턴 3: 가장 먼저 완료되는 작업 사용

```csharp
var task1 = GetDataFromServer1Async();
var task2 = GetDataFromServer2Async();

// 둘 중 먼저 완료되는 것 사용
var completedTask = await Task.WhenAny(task1, task2);
var result = await completedTask;
```

### 패턴 4: 타임아웃 처리

```csharp
var dataTask = GetDataAsync();
var timeoutTask = Task.Delay(5000);  // 5초 타임아웃

var completedTask = await Task.WhenAny(dataTask, timeoutTask);

if (completedTask == timeoutTask)
{
    Console.WriteLine("타임아웃 발생!");
}
else
{
    var data = await dataTask;
    Console.WriteLine("데이터 수신 완료");
}
```

---

## 8. 주의사항

### ❌ 하지 말아야 할 것

```csharp
// 1. async void 사용 금지 (이벤트 핸들러 제외)
async void BadMethod()  // ❌ 예외 처리 불가
{
    await Task.Delay(1000);
}

// 2. .Result나 .Wait() 사용 금지 (데드락 위험)
var result = GetDataAsync().Result;  // ❌ 데드락 가능
GetDataAsync().Wait();               // ❌ 데드락 가능

// 3. async 메서드에서 Thread.Sleep 사용 금지
async Task BadSleep()
{
    Thread.Sleep(1000);  // ❌ 스레드 블로킹
}
```

### ✅ 올바른 방법

```csharp
// 1. async Task 사용
async Task GoodMethod()  // ✅
{
    await Task.Delay(1000);
}

// 2. await 사용
var result = await GetDataAsync();  // ✅

// 3. Task.Delay 사용
async Task GoodSleep()
{
    await Task.Delay(1000);  // ✅ 논블로킹
}
```

---

## 9. 정리

| 항목 | 동기 (Sync) | 비동기 (Async) |
|------|-------------|----------------|
| **대기 방식** | 블로킹 (스레드 멈춤) | 논블로킹 (스레드 계속 작업) |
| **성능** | 순차 실행 (느림) | 동시 실행 (빠름) |
| **스레드** | 대기 중 점유 | 대기 중 해제 |
| **사용** | Thread.Sleep | await Task.Delay |
| **반환 타입** | void, T | Task, Task<T> |

**핵심 포인트:**
- `async`는 메서드에 붙이고, `await`는 호출할 때 사용
- `await` 없이 호출하면 작업이 백그라운드에서 시작됨
- `Task.WhenAll()`로 여러 작업을 동시에 실행
- `Task.Delay()`는 논블로킹, `Thread.Sleep()`은 블로킹

---

**수고하셨습니다! 🎉**
